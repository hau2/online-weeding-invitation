---
phase: 02-app-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/web/app/(admin)/admin/page.tsx
  - apps/web/components/admin/StatCard.tsx
  - apps/web/components/admin/AdminDashboardCharts.tsx
autonomous: false
requirements:
  - ADMN-01

must_haves:
  truths:
    - "Visiting /admin as a non-admin authenticated user is blocked at middleware (handled in 02-01)"
    - "Admin dashboard at /admin shows stat cards: total users, total invitations, active invitations, revenue summary"
    - "Admin dashboard has chart placeholder area below stat cards"
    - "Admin dashboard uses neutral white/gray theme — no pink/rose colors anywhere"
    - "Admin sidebar (built in 02-01) is visible and has all 8 nav items"
    - "Admin layout is completely separate from user app layout — no shared UI components"
  artifacts:
    - path: "apps/web/app/(admin)/admin/page.tsx"
      provides: "Admin dashboard page with stat cards and chart area"
      exports: ["default"]
    - path: "apps/web/components/admin/StatCard.tsx"
      provides: "Stat card component for admin dashboard (neutral gray theme)"
      exports: ["StatCard"]
    - path: "apps/web/components/admin/AdminDashboardCharts.tsx"
      provides: "Chart placeholder component (skeleton for Phase 8)"
      exports: ["AdminDashboardCharts"]
  key_links:
    - from: "apps/web/app/(admin)/admin/page.tsx"
      to: "apps/web/components/admin/StatCard.tsx"
      via: "import and render in top row"
      pattern: "StatCard"
    - from: "apps/web/app/(admin)/admin/page.tsx"
      to: "apps/web/components/admin/AdminDashboardCharts.tsx"
      via: "import and render below stat cards"
      pattern: "AdminDashboardCharts"
---

<objective>
Build the admin dashboard skeleton — stat cards and chart placeholder area — completing the admin layout started in Plan 02-01. This satisfies ADMN-01 by proving the /admin route is fully functional with a distinct visual identity.

Purpose: Phase 8 builds out the full admin panel on top of this skeleton. This plan creates the structural foundation and proves the admin route group works end-to-end.
Output: Admin dashboard page, StatCard component, chart placeholder — all in neutral white/gray, completely separate from the user-facing wedding theme.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-app-shell/02-CONTEXT.md
@.planning/phases/02-app-shell/02-RESEARCH.md

<interfaces>
<!-- Admin layout is built in 02-01: apps/web/app/(admin)/layout.tsx -->
<!-- AdminSidebar built in 02-01 with 8 nav items -->
<!-- This plan fills apps/web/app/(admin)/admin/page.tsx which 02-01 created as a stub -->

<!-- LOCKED DECISIONS (from CONTEXT.md):
  - Admin visual style: professional/neutral — clean white/gray theme, separate from pink wedding theme
  - Admin sidebar: icon + text for each of 8 fixed menu items (already built in 02-01)
  - Admin dashboard: stats cards + charts — top row stat cards for users/invitations/revenue, charts below
  - Admin panel must feel like a separate internal tool — clearly different visual identity
-->

<!-- Claude's Discretion: admin chart library choice, exact sidebar width -->
<!-- Use recharts for charts per RESEARCH.md recommendation -->
<!-- Stats in Phase 2 are hardcoded placeholders — real data comes from Phase 8 -->
<!-- Install recharts if not already installed -->
</interfaces>

@.planning/phases/02-app-shell/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatCard component and admin dashboard page</name>
  <files>
    apps/web/components/admin/StatCard.tsx,
    apps/web/components/admin/AdminDashboardCharts.tsx,
    apps/web/app/(admin)/admin/page.tsx
  </files>
  <action>
**Install recharts:**
```bash
pnpm --filter web add recharts
```

**StatCard.tsx** — Reusable stat card for admin dashboard. Neutral gray palette (NO pink):
```typescript
import { LucideIcon, TrendingUp } from 'lucide-react'
import { cn } from '@/lib/utils'

interface StatCardProps {
  title: string
  value: string | number
  description?: string
  icon: LucideIcon
  trend?: { value: number; label: string }
  className?: string
}

export function StatCard({ title, value, description, icon: Icon, trend, className }: StatCardProps) {
  return (
    <div className={cn(
      'bg-white rounded-xl border border-gray-200 p-6 flex flex-col gap-4 shadow-sm',
      className
    )}>
      <div className="flex items-start justify-between">
        <div>
          <p className="text-sm text-gray-500 font-medium">{title}</p>
          <p className="text-3xl font-bold text-gray-900 mt-1">{value}</p>
          {description && <p className="text-xs text-gray-400 mt-1">{description}</p>}
        </div>
        <div className="size-10 rounded-lg bg-gray-100 flex items-center justify-center flex-shrink-0">
          <Icon className="size-5 text-gray-600" />
        </div>
      </div>
      {trend && (
        <div className="flex items-center gap-1 text-xs text-gray-500">
          <TrendingUp className="size-3 text-green-500" />
          <span className="text-green-600 font-medium">{trend.value > 0 ? '+' : ''}{trend.value}%</span>
          <span>{trend.label}</span>
        </div>
      )}
    </div>
  )
}
```

**AdminDashboardCharts.tsx** — Chart placeholder using recharts. Renders a simple bar chart skeleton with placeholder data for Phase 2. Phase 8 replaces with real data:
```typescript
'use client'
import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer,
} from 'recharts'

const PLACEHOLDER_DATA = [
  { month: 'T1', invitations: 0 },
  { month: 'T2', invitations: 0 },
  { month: 'T3', invitations: 0 },
  { month: 'T4', invitations: 0 },
  { month: 'T5', invitations: 0 },
  { month: 'T6', invitations: 0 },
]

export function AdminDashboardCharts() {
  return (
    <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mt-6">
      {/* Invitations over time */}
      <div className="bg-white rounded-xl border border-gray-200 p-6 shadow-sm">
        <h3 className="text-sm font-semibold text-gray-700 mb-4">Thiệp cưới theo tháng</h3>
        <div className="h-48">
          <ResponsiveContainer width="100%" height="100%">
            <BarChart data={PLACEHOLDER_DATA} margin={{ top: 0, right: 0, bottom: 0, left: -20 }}>
              <CartesianGrid strokeDasharray="3 3" stroke="#f3f4f6" />
              <XAxis dataKey="month" tick={{ fontSize: 11, fill: '#9ca3af' }} />
              <YAxis tick={{ fontSize: 11, fill: '#9ca3af' }} />
              <Tooltip
                contentStyle={{ fontSize: 12, borderRadius: 8, border: '1px solid #e5e7eb' }}
              />
              <Bar dataKey="invitations" fill="#6b7280" radius={[4, 4, 0, 0]} />
            </BarChart>
          </ResponsiveContainer>
        </div>
        <p className="text-xs text-gray-400 mt-2 text-center">Dữ liệu thực tế sẽ có ở Phase 8</p>
      </div>

      {/* Revenue placeholder */}
      <div className="bg-white rounded-xl border border-gray-200 p-6 shadow-sm">
        <h3 className="text-sm font-semibold text-gray-700 mb-4">Doanh thu theo tháng</h3>
        <div className="h-48 flex items-center justify-center">
          <p className="text-sm text-gray-400">Biểu đồ doanh thu — Phase 8</p>
        </div>
      </div>
    </div>
  )
}
```

**Admin dashboard page** — replaces the stub created in 02-01. Shows 4 stat cards + charts. All placeholder data for Phase 2:
```typescript
import { Users, FileText, CheckCircle, DollarSign } from 'lucide-react'
import { StatCard } from '@/components/admin/StatCard'
import { AdminDashboardCharts } from '@/components/admin/AdminDashboardCharts'

const PLACEHOLDER_STATS = [
  {
    title: 'Tổng người dùng',
    value: '—',
    description: 'Phase 8 sẽ hiển thị số thực',
    icon: Users,
  },
  {
    title: 'Tổng thiệp cưới',
    value: '—',
    description: 'Phase 8 sẽ hiển thị số thực',
    icon: FileText,
  },
  {
    title: 'Đang hoạt động',
    value: '—',
    description: 'Thiệp đã xuất bản',
    icon: CheckCircle,
  },
  {
    title: 'Doanh thu',
    value: '—',
    description: 'Tổng doanh thu',
    icon: DollarSign,
  },
]

export default function AdminDashboardPage() {
  return (
    <div className="max-w-7xl mx-auto">
      <div className="mb-6">
        <h1 className="text-2xl font-semibold text-gray-900">Tổng quan hệ thống</h1>
        <p className="text-sm text-gray-500 mt-1">Dữ liệu thực tế sẽ được kết nối tại Phase 8</p>
      </div>

      {/* Stat cards row */}
      <div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4">
        {PLACEHOLDER_STATS.map((stat) => (
          <StatCard key={stat.title} {...stat} />
        ))}
      </div>

      {/* Charts area */}
      <AdminDashboardCharts />
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && pnpm --filter web build 2>&1 | tail -15</automated>
  </verify>
  <done>
    - /admin page renders 4 stat cards in a top row and chart area below
    - StatCard uses gray/white palette exclusively — no pink, no rose
    - AdminDashboardCharts renders a recharts BarChart with placeholder data
    - pnpm --filter web build exits 0 (no TypeScript errors)
    - Admin and user app layouts are visually and structurally separate (no shared layout.tsx, separate SidebarProviders)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of admin and app shells</name>
  <what-built>
    Complete admin skeleton: (admin) route group with neutral white/gray layout, AdminSidebar with 8 items (built in 02-01), stat card dashboard, and recharts placeholder charts. Also: (app) route group with rose/pink layout, AppSidebar, TopBar with 4-item user menu.
  </what-built>
  <how-to-verify>
    1. Start: `pnpm --filter web dev` and `pnpm --filter api dev`
    2. Visit http://localhost:3000/admin — confirm admin dashboard with gray stat cards and chart area. Verify NO pink/rose colors appear anywhere.
    3. Confirm AdminSidebar has all 8 items: Tổng quan, Người dùng, Thiệp cưới, Thư viện nhạc, Giao diện, Gói dịch vụ, Thanh toán, Cài đặt.
    4. Visit http://localhost:3000/dashboard (without session cookie) — should redirect to /dang-nhap.
    5. Confirm no "Xem QR" or QR-related button appears anywhere in the dashboard UI.
    6. Resize browser to 375px width on /admin — verify hamburger menu appears and sidebar becomes a slide-out sheet.
    7. Confirm sidebar collapse on desktop: clicking the trigger collapses to icon-only strip.
    8. Visit http://localhost:3000/dashboard with a valid session — confirm rose/pink sidebar and TopBar with: Tài khoản, Gói dịch vụ, Hỗ trợ, Đăng xuất.
  </how-to-verify>
  <action>Human visual verification — no code changes. Executor starts dev servers and follows verification steps above, then pauses for user confirmation.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; verification is manual per instructions above</automated>
  </verify>
  <done>User types "approved" confirming: admin uses gray/white theme with 8 nav items, app uses rose/pink theme with 4-item TopBar, sidebar collapse works, QR button absent from dashboard</done>
  <resume-signal>Type "approved" or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
Build and test:
```bash
pnpm --filter web build
pnpm --filter web test
```

Theme separation verified manually in checkpoint task above.
</verification>

<success_criteria>
- /admin page renders stat cards (neutral gray) + chart area — no pink colors
- Admin layout completely separate from app layout (separate SidebarProvider, cookieName="admin-sidebar:state")
- 8 admin nav items in AdminSidebar
- pnpm --filter web build exits 0
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-03-SUMMARY.md`
</output>
