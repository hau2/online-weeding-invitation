---
phase: 03-invitation-editor-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/templates/types.ts
  - apps/web/components/templates/TemplateTraditional.tsx
  - apps/web/components/templates/TemplateModern.tsx
  - apps/web/components/templates/TemplateMinimalist.tsx
  - apps/web/components/templates/TemplateRenderer.tsx
  - apps/web/components/templates/index.ts
  - apps/web/components/ui/accordion.tsx
  - apps/web/components/ui/textarea.tsx
  - apps/web/components/ui/collapsible.tsx
autonomous: true
requirements: [EDIT-08]

must_haves:
  truths:
    - "Each template renders all invitation fields (names, date, time, venue, message, thank-you) in its distinct visual style"
    - "TemplateRenderer maps templateId to the correct component and renders it"
    - "Templates accept Invitation data as props and have zero side effects (no fetching, no state mutation)"
    - "Traditional template uses gold/red/burgundy colors with ornate feel"
    - "Modern template uses white/rose-gold with clean minimalist layout"
    - "Minimalist template uses cream/off-white with pure typography focus"
  artifacts:
    - path: "apps/web/components/templates/types.ts"
      provides: "TemplateProps interface"
      exports: ["TemplateProps"]
    - path: "apps/web/components/templates/TemplateTraditional.tsx"
      provides: "Traditional template component (gold/red)"
      exports: ["TemplateTraditional"]
    - path: "apps/web/components/templates/TemplateModern.tsx"
      provides: "Modern template component (white/rose-gold)"
      exports: ["TemplateModern"]
    - path: "apps/web/components/templates/TemplateMinimalist.tsx"
      provides: "Minimalist template component (cream/typography)"
      exports: ["TemplateMinimalist"]
    - path: "apps/web/components/templates/TemplateRenderer.tsx"
      provides: "Dynamic template selector by templateId"
      exports: ["TemplateRenderer"]
    - path: "apps/web/components/templates/index.ts"
      provides: "Barrel export for all template components"
  key_links:
    - from: "TemplateRenderer.tsx"
      to: "TemplateTraditional/Modern/Minimalist"
      via: "TEMPLATES record mapping templateId to component"
      pattern: "TEMPLATES\\[.*templateId\\]"
    - from: "types.ts"
      to: "@repo/types Invitation"
      via: "import type"
      pattern: "import type.*Invitation.*from '@repo/types'"
---

<objective>
Build the 3 shared invitation template components (Traditional, Modern, Minimalist) and the TemplateRenderer that maps templateId to the correct component. Also install the missing shadcn/ui components (accordion, textarea, collapsible) needed by the editor form in Plan 03.

Purpose: Template components are the visual core of the product -- they render identically in the editor preview (Phase 3) and the public page (Phase 5). They must be pure presentational components accepting Invitation data as props with zero data fetching or state management.

Output: 5 template files in components/templates/, 3 new shadcn/ui components installed, barrel export.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-invitation-editor-core/03-CONTEXT.md

@packages/types/src/invitation.ts
@apps/web/lib/fonts.ts
@apps/web/app/globals.css
@apps/web/components/ui/typography.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/types/src/invitation.ts:
```typescript
export type InvitationStatus = 'draft' | 'published' | 'expired'
export type TemplateId = 'traditional' | 'modern' | 'minimalist'

export interface Invitation {
  id: string; userId: string; slug: string | null;
  status: InvitationStatus; templateId: TemplateId;
  groomName: string; brideName: string;
  weddingDate: string | null; weddingTime: string | null;
  venueName: string; venueAddress: string;
  invitationMessage: string; thankYouText: string;
  createdAt: string; updatedAt: string; deletedAt: string | null;
}
```

Available fonts (from apps/web/lib/fonts.ts):
- `--font-be-vietnam-pro` (body text, 300-700)
- `--font-playfair-display` (serif headings, 400-700, italic)
- `--font-dancing-script` (cursive decorative, 400-700)

Color palette (from globals.css @theme):
- Rose palette: rose-50 through rose-900
- Gold palette: gold-200 through gold-600
- Cream palette: cream-50 through cream-200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components and create template type contract</name>
  <files>
    apps/web/components/ui/accordion.tsx,
    apps/web/components/ui/textarea.tsx,
    apps/web/components/ui/collapsible.tsx,
    apps/web/components/templates/types.ts
  </files>
  <action>
    1. Install shadcn/ui components needed by the editor form (Plan 03 will consume these):
       ```bash
       cd apps/web && npx shadcn@latest add accordion textarea collapsible --yes
       ```
       These will be installed with base-nova style using @base-ui/react primitives (matching project's existing shadcn config in components.json).

    2. Create `apps/web/components/templates/types.ts`:
       ```typescript
       import type { Invitation } from '@repo/types'

       export interface TemplateProps {
         invitation: Invitation
         className?: string
       }
       ```
       This is the contract that ALL template components and the TemplateRenderer implement. Phase 5 public page will also import this.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && ls components/ui/accordion.tsx components/ui/textarea.tsx components/ui/collapsible.tsx components/templates/types.ts && echo "All files exist"</automated>
  </verify>
  <done>
    - accordion.tsx, textarea.tsx, collapsible.tsx installed in components/ui/
    - TemplateProps interface exported from components/templates/types.ts
    - No TypeScript errors in the type file
  </done>
</task>

<task type="auto">
  <name>Task 2: Three template components + TemplateRenderer + barrel export</name>
  <files>
    apps/web/components/templates/TemplateTraditional.tsx,
    apps/web/components/templates/TemplateModern.tsx,
    apps/web/components/templates/TemplateMinimalist.tsx,
    apps/web/components/templates/TemplateRenderer.tsx,
    apps/web/components/templates/index.ts
  </files>
  <action>
    Build 3 template components as pure presentational 'use client' components. Each accepts `TemplateProps` and renders all invitation fields in its visual style. All must handle empty/null fields gracefully (show placeholder text or hide section).

    **Date/time formatting:** Use `new Intl.DateTimeFormat('vi-VN', { dateStyle: 'full' })` for weddingDate display. Format weddingTime as-is (HH:MM string). If weddingDate is null, show a placeholder like "Chưa chọn ngày".

    **TemplateTraditional.tsx** -- Ornate gold and red, formal Vietnamese wedding:
    - Background: deep red/burgundy gradient (from-red-900 to-red-800 or custom)
    - Text: gold/warm tones
    - Double happiness character (囍) as decorative element
    - Decorative borders or ornamental dividers between sections
    - Use Playfair Display (--font-playfair-display) for couple names heading
    - Use Dancing Script (--font-dancing-script) for the "&" connector and decorative text
    - Sections: couple names at top, wedding date/time, venue, invitation message, thank-you text at bottom
    - Generous padding, formal spacing

    **TemplateModern.tsx** -- Clean white + rose gold, Instagram-worthy:
    - Background: white or very light rose (rose-50)
    - Accent: rose-gold tones (use rose-300/rose-400)
    - Minimalist layout with generous whitespace
    - Modern sans-serif for body (Be Vietnam Pro is already the base font)
    - Use Playfair Display for couple names
    - Thin rose-gold horizontal lines as section dividers
    - Clean, centered layout
    - Subtle shadow or card-like feel

    **TemplateMinimalist.tsx** -- Pure typography on cream:
    - Background: cream/off-white (cream-50 or similar warm white)
    - Text: dark gray/charcoal (not pure black)
    - Almost no decorative elements
    - Beautiful typography hierarchy is the entire design
    - Use Playfair Display for names (large, bold)
    - Thin hairline dividers only
    - Maximum breathing room between sections
    - Quiet elegance

    **TemplateRenderer.tsx:**
    - Import all 3 template components
    - Create `TEMPLATES: Record<TemplateId, React.ComponentType<TemplateProps>>` mapping
    - Export `TemplateRenderer` component that looks up `invitation.templateId` in the map and renders the appropriate component
    - Fall back to TemplateTraditional if templateId is somehow invalid

    **index.ts (barrel export):**
    - Re-export TemplateProps, TemplateRenderer, and all 3 individual templates
    - Re-export TemplateId from @repo/types for convenience

    IMPORTANT: Templates must be 'use client' components (they use CSS custom properties for fonts which resolve client-side). They must NOT fetch data, manage state, or have side effects. They are pure render functions.

    IMPORTANT: Handle null/empty fields gracefully. When groomName/brideName are empty strings, show "Chú rể" / "Cô dâu" as defaults. When date/time are null, show "Chưa chọn ngày" / "Chưa chọn giờ". When message/thankYou are empty, show default Vietnamese text.

    Each template should be a complete visual design, approximately 80-150 lines of JSX+Tailwind. The templates should look polished -- couples will judge the product quality by the preview.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20 || echo "TypeScript check complete"</automated>
  </verify>
  <done>
    - TemplateTraditional renders gold/red ornate design with all invitation fields
    - TemplateModern renders clean white/rose-gold design with all invitation fields
    - TemplateMinimalist renders pure typography on cream with all invitation fields
    - TemplateRenderer maps templateId to correct component, falls back to Traditional
    - All templates handle null/empty fields with Vietnamese placeholder text
    - All templates are 'use client' with zero data fetching
    - Barrel export re-exports all components and types
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json
```
TypeScript compiles. All template files exist. TemplateRenderer renders each template by ID.
</verification>

<success_criteria>
- 3 visually distinct template components exist with correct color schemes per user decision
- TemplateRenderer dynamically renders the correct template by templateId
- All templates accept TemplateProps (Invitation + className) as their only interface
- Templates handle empty/null invitation fields gracefully
- shadcn accordion, textarea, collapsible installed and available for Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/03-invitation-editor-core/03-02-SUMMARY.md`
</output>
