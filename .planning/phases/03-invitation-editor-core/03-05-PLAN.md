---
phase: 03-invitation-editor-core
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx
  - apps/web/app/(app)/thep-cuoi/[id]/PublishButton.tsx
autonomous: true
gap_closure: true
requirements:
  - EDIT-09

must_haves:
  truths:
    - "User can open the full preview dialog from a draft (unpublished) invitation"
    - "User can open the full preview dialog from a published invitation"
    - "The preview shows the complete invitation using TemplateRenderer"
  artifacts:
    - path: "apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx"
      provides: "Standalone preview button in topbar visible regardless of publish status"
      contains: "setShowFullPreview(true)"
    - path: "apps/web/app/(app)/thep-cuoi/[id]/PublishButton.tsx"
      provides: "Published-state dropdown retains existing Xem truoc option"
  key_links:
    - from: "apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx"
      to: "apps/web/app/(app)/thep-cuoi/[id]/FullPreviewDialog.tsx"
      via: "setShowFullPreview(true) on button click"
      pattern: "setShowFullPreview\\(true\\)"
---

<objective>
Close verification gap for EDIT-09: "User can preview the complete invitation as a guest would see it before publishing."

The FullPreviewDialog component is fully implemented but the only UI trigger ("Xem truoc" in PublishButton's dropdown) is gated on `isPublished`. Draft invitations have no way to open the full preview. The fix adds a standalone preview button in the editor topbar that is always visible, regardless of publication status.

Purpose: Unblock EDIT-09 so Phase 3 verification passes 5/5 truths.
Output: Modified EditorShell.tsx with a preview button in the topbar; PublishButton.tsx cleaned up to remove the now-redundant preview dropdown item for published state.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-invitation-editor-core/03-VERIFICATION.md

@apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx
@apps/web/app/(app)/thep-cuoi/[id]/PublishButton.tsx
@apps/web/app/(app)/thep-cuoi/[id]/FullPreviewDialog.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From EditorShell.tsx (current state):
- `showFullPreview` state and `setShowFullPreview` are already declared at line 31
- `FullPreviewDialog` is already rendered at line 82-86 with open={showFullPreview} and onOpenChange={setShowFullPreview}
- The topbar div is at lines 64-80; the `<div className="flex-1" />` spacer is at line 73
- `PublishButton` receives `onPreview={() => setShowFullPreview(true)}` at line 78

From PublishButton.tsx (current state):
- `onPreview?: () => void` is an optional prop (line 29)
- The dropdown with "Xem truoc" is at lines 158-182, gated on `{isPublished && <DropdownMenu>...}`
- The dropdown also contains the "Huy xuat ban" item — this must remain accessible for published invitations

From FullPreviewDialog.tsx:
- Props: `{ invitation: Invitation, open: boolean, onOpenChange: (open: boolean) => void }`
- Renders TemplateRenderer inside a scrollable Dialog — no changes needed to this file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add standalone preview button to editor topbar and clean up PublishButton dropdown</name>
  <files>apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx, apps/web/app/(app)/thep-cuoi/[id]/PublishButton.tsx</files>
  <action>
In EditorShell.tsx:
1. Import the `Eye` icon from `lucide-react`
2. Add a preview button in the topbar div (between the `<div className="flex-1" />` spacer and the `<PublishButton .../>` component). The button should:
   - Use `<Button variant="ghost" size="sm" onClick={() => setShowFullPreview(true)}>`
   - Render `<Eye className="size-3.5" />` icon and text "Xem truoc"
   - Be always visible regardless of `invitation.status`
   - Use consistent styling: `text-rose-600 hover:text-rose-700 hover:bg-rose-50` classes
3. Remove the `onPreview` prop from the `<PublishButton>` usage since the standalone button replaces it

In PublishButton.tsx:
1. Remove the `onPreview` prop from the `PublishButtonProps` interface and the destructured props
2. In the published-state dropdown (lines 158-182), remove the "Xem truoc" `<DropdownMenuItem>` and its associated `<DropdownMenuSeparator>`. Keep only the "Huy xuat ban" item in the dropdown.
3. The dropdown itself remains gated on `isPublished` since it still contains the "Huy xuat ban" action

Important: Do NOT modify FullPreviewDialog.tsx — it already works correctly.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && grep -n "setShowFullPreview(true)" apps/web/app/\(app\)/thep-cuoi/\[id\]/EditorShell.tsx | grep -v "onPreview" && grep -c "onPreview" apps/web/app/\(app\)/thep-cuoi/\[id\]/PublishButton.tsx | grep "0" && echo "PASS: Preview button in EditorShell, onPreview removed from PublishButton"</automated>
  </verify>
  <done>
- A preview button with Eye icon and "Xem truoc" text is visible in the editor topbar for ALL invitations (draft AND published)
- Clicking the button opens the FullPreviewDialog showing the complete invitation via TemplateRenderer
- The PublishButton no longer has an onPreview prop or a "Xem truoc" dropdown item
- The published-state dropdown still contains "Huy xuat ban" for unpublishing
- TypeScript compiles without errors (`npx tsc --noEmit` in apps/web)
  </done>
</task>

</tasks>

<verification>
1. `grep -n "setShowFullPreview(true)" apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx` — shows the standalone button click handler
2. `grep -c "onPreview" apps/web/app/(app)/thep-cuoi/[id]/PublishButton.tsx` — returns 0 (prop removed)
3. `grep -c "onPreview" apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx` — returns 0 (prop usage removed)
4. `cd apps/web && npx tsc --noEmit` — TypeScript compiles cleanly
5. `cd apps/web && npx vitest run` — all existing tests pass (no regressions)
</verification>

<success_criteria>
- EDIT-09 gap is closed: a user can preview the complete invitation as a guest would see it at any time, not just after publishing
- The preview button is always visible in the editor topbar
- No regressions to publish/unpublish flow (EDIT-10 still works)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-invitation-editor-core/03-05-SUMMARY.md`
</output>
