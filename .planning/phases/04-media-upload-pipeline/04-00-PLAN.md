---
phase: 04-media-upload-pipeline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/__tests__/components/PhotoGallery.test.tsx
  - apps/web/__tests__/components/MusicPicker.test.tsx
  - apps/web/__tests__/components/BankQrUpload.test.tsx
  - apps/api/src/invitations/pipes/image-optimization.pipe.spec.ts
  - apps/api/src/invitations/__tests__/invitations.service.spec.ts
autonomous: true
requirements: [EDIT-04, EDIT-05, EDIT-06, EDIT-07]

must_haves:
  truths:
    - "Test stub files exist for all 4 phase requirements before any production code is written"
    - "All test stubs run without errors (pending/skipped status, not failures)"
    - "Existing invitations.service.spec.ts tests still pass after extension"
  artifacts:
    - path: "apps/web/__tests__/components/PhotoGallery.test.tsx"
      provides: "Stub tests for EDIT-04 photo gallery component"
      contains: "describe.*PhotoGallery"
    - path: "apps/web/__tests__/components/MusicPicker.test.tsx"
      provides: "Stub tests for EDIT-06 music picker component"
      contains: "describe.*MusicPicker"
    - path: "apps/web/__tests__/components/BankQrUpload.test.tsx"
      provides: "Stub tests for EDIT-07 bank QR upload component"
      contains: "describe.*BankQrUpload"
    - path: "apps/api/src/invitations/pipes/image-optimization.pipe.spec.ts"
      provides: "Stub tests for EDIT-05 image optimization (processImage)"
      contains: "describe.*processImage"
    - path: "apps/api/src/invitations/__tests__/invitations.service.spec.ts"
      provides: "Extended with media upload test stubs"
      contains: "uploadPhotos"
  key_links: []
---

<objective>
Create test stub files for all Phase 4 requirements (EDIT-04 through EDIT-07) so that subsequent plans have automated verify targets from day one. This is the Nyquist Wave 0 requirement: test scaffolds must exist before production code.

Purpose: Ensures every subsequent plan task can include a vitest automated verify command. Without these stubs, plans 04-01, 04-02, and 04-03 have no test targets and fail Nyquist compliance.

Output: 5 test files (3 new web component tests, 1 new API pipe test, 1 extended API service test) all in pending/todo state.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-media-upload-pipeline/04-CONTEXT.md
@.planning/phases/04-media-upload-pipeline/04-VALIDATION.md

<interfaces>
<!-- Test patterns from existing codebase -->

From apps/web/__tests__/components/EditorForm.test.tsx (stub pattern):
```typescript
import { describe, it } from 'vitest'

describe('EditorForm', () => {
  it.todo('renders 3 accordion sections: couple, ceremony, message')
  // ... more it.todo stubs
})
```

From apps/api/src/invitations/__tests__/invitations.service.spec.ts (full test pattern):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { NotFoundException } from '@nestjs/common'
import { InvitationsService } from '../invitations.service'
// ... uses makeSupabaseMock, makeService helpers
```

API test config: `apps/api/vitest.config.ts` includes `src/**/*.spec.ts`
Web test config: `apps/web/vitest.config.ts` includes `__tests__/**/*.{test,spec}.{ts,tsx}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web component test stubs (PhotoGallery, MusicPicker, BankQrUpload)</name>
  <files>
    apps/web/__tests__/components/PhotoGallery.test.tsx
    apps/web/__tests__/components/MusicPicker.test.tsx
    apps/web/__tests__/components/BankQrUpload.test.tsx
  </files>
  <action>
Create 3 test stub files using the `it.todo()` pattern established in `EditorForm.test.tsx`.

**Create `apps/web/__tests__/components/PhotoGallery.test.tsx`:**
```typescript
import { describe, it } from 'vitest'

describe('PhotoGallery', () => {
  // EDIT-04a: Upload
  it.todo('renders a drop zone with "Tha anh vao day" text when dragging files over')
  it.todo('calls apiUpload with FormData containing selected files on file input change')
  it.todo('calls onChange with updated photoUrls array on successful upload')
  it.todo('shows toast error on upload failure')
  it.todo('disables add button and shows message when 10 photos reached')

  // EDIT-04b: Rendering
  it.todo('renders thumbnails in a 3-column grid matching photoUrls order')
  it.todo('renders drag handle and delete button on each thumbnail')
  it.todo('renders "+" add button as last grid item when under 10 photos')

  // EDIT-04c: Reorder
  it.todo('calls onChange with reordered array after drag-end')

  // EDIT-04d: Delete
  it.todo('calls apiFetch DELETE for the photo at the given index')
  it.todo('calls onChange with updated photoUrls on successful delete')
})
```

**Create `apps/web/__tests__/components/MusicPicker.test.tsx`:**
```typescript
import { describe, it } from 'vitest'

describe('MusicPicker', () => {
  // EDIT-06a: Track list
  it.todo('fetches and renders system music tracks on mount')
  it.todo('displays track title, artist, and formatted duration')

  // EDIT-06b: Selection
  it.todo('highlights the currently selected track with a visual indicator')
  it.todo('calls onSelect with track ID when clicking an unselected track')
  it.todo('calls onSelect with null when clicking the already-selected track')

  // Preview (manual verify -- stubs for structure only)
  it.todo('creates Howl instance and plays on preview button click')
  it.todo('stops previous preview when starting a new one')
  it.todo('cleans up Howl instance on unmount')
})
```

**Create `apps/web/__tests__/components/BankQrUpload.test.tsx`:**
```typescript
import { describe, it } from 'vitest'

describe('BankQrUpload', () => {
  // EDIT-07a: Upload
  it.todo('renders upload zone when no bankQrUrl exists')
  it.todo('renders QR preview image when bankQrUrl exists')
  it.todo('calls apiUpload with FormData on file select')
  it.todo('calls onChange with bankQrUrl on successful upload')

  // EDIT-07b: Bank fields
  it.todo('renders "Ten ngan hang" input with current bankName value')
  it.todo('renders "Chu tai khoan" input with current bankAccountHolder value')
  it.todo('calls onChange with bankName on bank name input change')
  it.todo('calls onChange with bankAccountHolder on account holder input change')
})
```

All files use `it.todo()` which registers as pending tests that do not fail the suite.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && npx vitest run --reporter=verbose 2>&1 | grep -E "PhotoGallery|MusicPicker|BankQrUpload|todo|skipped"</automated>
  </verify>
  <done>Three test stub files exist in apps/web/__tests__/components/. All stubs appear as todo/skipped in vitest output. No test failures introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Create API test stubs (image-optimization pipe, extend invitations.service.spec)</name>
  <files>
    apps/api/src/invitations/pipes/image-optimization.pipe.spec.ts
    apps/api/src/invitations/__tests__/invitations.service.spec.ts
  </files>
  <action>
**Create `apps/api/src/invitations/pipes/image-optimization.pipe.spec.ts`:**

Create the `pipes/` directory if it does not exist. This tests the `processImage` method which will be added to InvitationsService in Plan 04-01.

```typescript
import { describe, it } from 'vitest'

describe('processImage (image optimization)', () => {
  // EDIT-05a: Valid image processing
  it.todo('converts a valid JPEG buffer to WebP format')
  it.todo('resizes image to max 1200px width without enlargement')
  it.todo('produces WebP output at quality 75')
  it.todo('auto-rotates from EXIF metadata via sharp resize')

  // EDIT-05b: Magic-byte validation
  it.todo('accepts JPEG magic bytes (FF D8 FF)')
  it.todo('accepts PNG magic bytes (89 50 4E 47)')
  it.todo('accepts WebP magic bytes (52 49 46 46)')
  it.todo('rejects PDF file disguised with .jpg extension')
  it.todo('rejects empty buffer')
  it.todo('throws BadRequestException with Vietnamese message for invalid format')
})
```

**Extend `apps/api/src/invitations/__tests__/invitations.service.spec.ts`:**

Add new describe blocks at the end of the file (AFTER the existing `slug immutability` describe block) with todo stubs for media upload service methods:

```typescript
describe('uploadPhotos', () => {
  it.todo('uploads files to Supabase Storage and appends URLs to photo_urls')
  it.todo('enforces 10-photo maximum including existing photos')
  it.todo('throws NotFoundException for non-owner')
  it.todo('processes each file through processImage before upload')
})

describe('deletePhoto', () => {
  it.todo('removes photo URL at given index from photo_urls array')
  it.todo('removes file from Supabase Storage')
  it.todo('throws BadRequestException for out-of-bounds index')
  it.todo('throws NotFoundException for non-owner')
})

describe('uploadBankQr', () => {
  it.todo('uploads QR image and sets bank_qr_url on invitation')
  it.todo('deletes old QR file when replacing with new one')
  it.todo('throws NotFoundException for non-owner')
})

describe('updatePhotoOrder', () => {
  it.todo('updates photo_urls array with new order')
  it.todo('validates all URLs are existing Supabase Storage URLs')
  it.todo('throws NotFoundException for non-owner')
})

describe('listMusicTracks', () => {
  it.todo('returns active tracks ordered by sort_order')
  it.todo('maps snake_case columns to camelCase SystemMusicTrack')
})
```

IMPORTANT: Do NOT modify any existing test code. Only append new describe blocks after the closing of the last existing describe block.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/api && npx vitest run --reporter=verbose 2>&1 | grep -E "processImage|uploadPhotos|deletePhoto|uploadBankQr|listMusicTracks|updatePhotoOrder|todo|skipped|PASS|FAIL"</automated>
  </verify>
  <done>API test stubs exist for processImage (image optimization), uploadPhotos, deletePhoto, uploadBankQr, updatePhotoOrder, and listMusicTracks. All existing tests still pass. New stubs appear as todo/skipped.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vitest run --reporter=verbose` -- all existing tests pass, new stubs show as todo
- `cd apps/api && npx vitest run --reporter=verbose` -- all existing tests pass, new stubs show as todo
- `ls apps/web/__tests__/components/PhotoGallery.test.tsx apps/web/__tests__/components/MusicPicker.test.tsx apps/web/__tests__/components/BankQrUpload.test.tsx` -- all 3 files exist
- `ls apps/api/src/invitations/pipes/image-optimization.pipe.spec.ts` -- pipe test exists
- `grep "uploadPhotos\|deletePhoto\|uploadBankQr\|updatePhotoOrder\|listMusicTracks" apps/api/src/invitations/__tests__/invitations.service.spec.ts` -- media stubs present
</verification>

<success_criteria>
- 5 test files created/extended with todo stubs covering EDIT-04 through EDIT-07
- Zero test failures (all new tests are `it.todo()`)
- All existing tests continue to pass unchanged
- Plans 04-01, 04-02, and 04-03 now have valid vitest targets for their verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/04-media-upload-pipeline/04-00-SUMMARY.md`
</output>
