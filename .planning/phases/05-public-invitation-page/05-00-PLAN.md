---
phase: 05-public-invitation-page
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/invitations/__tests__/public-invitations.spec.ts
  - apps/api/src/invitations/__tests__/qr-generation.spec.ts
  - apps/web/__tests__/components/EnvelopeAnimation.test.tsx
  - apps/web/__tests__/components/GuestName.test.tsx
  - apps/web/__tests__/components/InvitationContent.test.tsx
  - apps/web/__tests__/components/CountdownTimer.test.tsx
  - apps/web/__tests__/components/MusicPlayer.test.tsx
  - apps/web/__tests__/components/ThankYouPage.test.tsx
  - apps/api/package.json
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
  - PUBL-03
  - PUBL-04
  - PUBL-05
  - PUBL-06
  - PUBL-07
  - PUBL-08
  - PUBL-11
  - PUBL-12

must_haves:
  truths:
    - "All test stub files exist and import correctly"
    - "qrcode dependency is installed in apps/api"
    - "Running test suite produces pending/todo results, not errors"
  artifacts:
    - path: "apps/api/src/invitations/__tests__/public-invitations.spec.ts"
      provides: "Test stubs for PUBL-01, PUBL-11"
      contains: "it.todo"
    - path: "apps/api/src/invitations/__tests__/qr-generation.spec.ts"
      provides: "Test stubs for PUBL-02"
      contains: "it.todo"
    - path: "apps/web/__tests__/components/EnvelopeAnimation.test.tsx"
      provides: "Test stubs for PUBL-03, PUBL-04"
      contains: "it.todo"
    - path: "apps/web/__tests__/components/MusicPlayer.test.tsx"
      provides: "Test stubs for PUBL-08"
      contains: "it.todo"
    - path: "apps/web/__tests__/components/CountdownTimer.test.tsx"
      provides: "Test stubs for PUBL-07"
      contains: "it.todo"
    - path: "apps/web/__tests__/components/ThankYouPage.test.tsx"
      provides: "Test stubs for PUBL-12"
      contains: "it.todo"
  key_links: []
---

<objective>
Wave 0: Create test stub scaffolds for all Phase 5 requirements and install the qrcode dependency.

Purpose: Nyquist compliance -- every subsequent plan has failing test targets to turn green. The qrcode package is needed for QR generation in Plan 05-01.
Output: 8 test stub files (6 web, 2 API) with it.todo() stubs, qrcode installed in apps/api.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-invitation-page/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install qrcode dependency and create API test stubs</name>
  <files>
    apps/api/package.json,
    apps/api/src/invitations/__tests__/public-invitations.spec.ts,
    apps/api/src/invitations/__tests__/qr-generation.spec.ts
  </files>
  <action>
    1. Install qrcode in apps/api:
       cd apps/api && pnpm add qrcode && pnpm add -D @types/qrcode

    2. Create apps/api/src/invitations/__tests__/public-invitations.spec.ts with it.todo() stubs:
       - "should return published invitation by slug without authentication" (PUBL-01)
       - "should return 404 for non-existent slug" (PUBL-01)
       - "should return 404 for unpublished/draft invitation slug" (PUBL-01)
       - "should return expired state with thank-you content after grace period" (PUBL-11)
       - "should return normal invitation during grace period" (PUBL-11)
       - "should include music track URL when musicTrackId is set" (PUBL-06)

    3. Create apps/api/src/invitations/__tests__/qr-generation.spec.ts with it.todo() stubs:
       - "should generate QR code PNG buffer for invitation URL" (PUBL-02)
       - "should upload QR to Supabase Storage" (PUBL-02)
       - "should not regenerate QR if slug already exists on re-publish" (PUBL-02)
       - "should use SITE_URL env var for QR URL" (PUBL-02)

    Follow the existing pattern in apps/api/src/invitations/__tests__/ for describe block structure.
    Use `import { describe, it } from 'vitest'` (no expect needed for todo stubs).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/api && pnpm vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>qrcode and @types/qrcode in api package.json, 2 test files with 10 it.todo() stubs total, no test errors</done>
</task>

<task type="auto">
  <name>Task 2: Create web component test stubs</name>
  <files>
    apps/web/__tests__/components/EnvelopeAnimation.test.tsx,
    apps/web/__tests__/components/GuestName.test.tsx,
    apps/web/__tests__/components/InvitationContent.test.tsx,
    apps/web/__tests__/components/CountdownTimer.test.tsx,
    apps/web/__tests__/components/MusicPlayer.test.tsx,
    apps/web/__tests__/components/ThankYouPage.test.tsx
  </files>
  <action>
    Create 6 test stub files in apps/web/__tests__/components/ with it.todo() stubs.
    Follow the existing pattern from Phase 4 test stubs (e.g., apps/web/__tests__/components/PhotoGallery.test.tsx).
    Use `import { describe, it } from 'vitest'`.

    1. EnvelopeAnimation.test.tsx:
       - "should render sealed envelope with wax seal" (PUBL-03)
       - "should show guest name on envelope when ?to= param provided" (PUBL-05 partial)
       - "should trigger reveal animation on tap/click" (PUBL-04)
       - "should match template color scheme" (PUBL-03)
       - "should render skip button" (PUBL-03)

    2. GuestName.test.tsx:
       - "should display personalized greeting with guest name from ?to= param" (PUBL-05)
       - "should show generic greeting when no ?to= param" (PUBL-05)
       - "should truncate name at 50 characters" (PUBL-05)
       - "should strip HTML tags from name" (PUBL-05)

    3. InvitationContent.test.tsx:
       - "should render TemplateRenderer with invitation data" (PUBL-06)
       - "should display all invitation fields: names, date, venue, message" (PUBL-06)

    4. CountdownTimer.test.tsx:
       - "should display days, hours, minutes, seconds" (PUBL-07)
       - "should update countdown every second" (PUBL-07)
       - "should hide after wedding date passes" (PUBL-07)
       - "should use Vietnam timezone for calculation" (PUBL-07)

    5. MusicPlayer.test.tsx:
       - "should render floating play/pause button" (PUBL-08)
       - "should not autoplay on mount" (PUBL-08)
       - "should show equalizer bars when playing" (PUBL-08)
       - "should toggle play/pause on click" (PUBL-08)

    6. ThankYouPage.test.tsx:
       - "should display couple names and thank-you text" (PUBL-12)
       - "should show first photo from gallery" (PUBL-12)
       - "should not show bank QR or countdown" (PUBL-12)
       - "should not show envelope animation" (PUBL-12)
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && pnpm vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>6 test files with ~24 it.todo() stubs total, no test errors, all show as pending/todo in test output</done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm vitest run --reporter=verbose` -- no failures, todo stubs listed
- `cd apps/web && pnpm vitest run --reporter=verbose` -- no failures, todo stubs listed
- `cat apps/api/package.json | grep qrcode` -- shows qrcode dependency
</verification>

<success_criteria>
- All 8 test stub files exist with it.todo() entries
- qrcode and @types/qrcode installed in apps/api
- Test suite runs without errors (todos are not errors)
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-invitation-page/05-00-SUMMARY.md`
</output>
