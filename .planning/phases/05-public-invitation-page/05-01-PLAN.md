---
phase: 05-public-invitation-page
plan: 01
type: execute
wave: 1
depends_on:
  - 05-00
files_modified:
  - apps/api/src/invitations/public-invitations.controller.ts
  - apps/api/src/invitations/invitations.service.ts
  - apps/api/src/invitations/invitations.module.ts
  - apps/api/src/invitations/__tests__/public-invitations.spec.ts
  - apps/api/src/invitations/__tests__/qr-generation.spec.ts
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
  - PUBL-11
  - PUBL-12

must_haves:
  truths:
    - "GET /invitations/public/:slug returns published invitation without JWT"
    - "GET /invitations/public/:slug returns 404 for draft/deleted/nonexistent slugs"
    - "Response includes expired flag and thank-you content when past grace period"
    - "QR code PNG is generated and stored to Supabase Storage on first publish"
    - "Re-publish does not regenerate QR if slug already exists"
  artifacts:
    - path: "apps/api/src/invitations/public-invitations.controller.ts"
      provides: "Unauthenticated public endpoint"
      exports: ["PublicInvitationsController"]
    - path: "apps/api/src/invitations/invitations.service.ts"
      provides: "findBySlug and generateQrCode methods"
      contains: "findBySlug"
  key_links:
    - from: "apps/api/src/invitations/public-invitations.controller.ts"
      to: "apps/api/src/invitations/invitations.service.ts"
      via: "findBySlug method call"
      pattern: "invitationsService\\.findBySlug"
    - from: "apps/api/src/invitations/invitations.service.ts"
      to: "Supabase Storage qr-codes bucket"
      via: "QR upload in publish method"
      pattern: "from\\('qr-codes'\\)"
---

<objective>
Create the public NestJS API layer: a separate controller for unauthenticated access to published invitations by slug, QR code generation at publish time, and expiry/thank-you logic.

Purpose: This is the backend foundation for the entire public page. The public endpoint serves data to the Next.js ISR page, and the QR code gives couples a scannable link to share.
Output: PublicInvitationsController, findBySlug service method, generateQrCode service method, QR generation wired into publish flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-invitation-page/05-CONTEXT.md
@.planning/phases/05-public-invitation-page/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/types/src/invitation.ts:
```typescript
export type InvitationStatus = 'draft' | 'published' | 'expired'
export type TemplateId = 'traditional' | 'modern' | 'minimalist'

export interface Invitation {
  id: string
  userId: string
  slug: string | null
  status: InvitationStatus
  templateId: TemplateId
  groomName: string
  brideName: string
  weddingDate: string | null
  weddingTime: string | null
  venueName: string
  venueAddress: string
  venueMapUrl: string
  invitationMessage: string
  thankYouText: string
  photoUrls: string[]
  musicTrackId: string | null
  bankQrUrl: string | null
  bankName: string
  bankAccountHolder: string
  brideBankQrUrl: string | null
  brideBankName: string
  brideBankAccountHolder: string
  createdAt: string
  updatedAt: string
  deletedAt: string | null
}
```

From apps/api/src/invitations/invitations.controller.ts:
```typescript
@Controller('invitations')
@UseGuards(JwtGuard)  // <-- class-level guard means public endpoint CANNOT go here
export class InvitationsController { ... }
```

From apps/api/src/invitations/invitations.service.ts:
```typescript
// mapRow, SELECT_ALL, InvitationRow, MusicTrackRow already defined
// publish() method generates slug -- QR generation hooks in here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public endpoint — findBySlug with expiry logic</name>
  <files>
    apps/api/src/invitations/public-invitations.controller.ts,
    apps/api/src/invitations/invitations.service.ts,
    apps/api/src/invitations/invitations.module.ts,
    apps/api/src/invitations/__tests__/public-invitations.spec.ts
  </files>
  <behavior>
    - findBySlug("valid-slug") returns published invitation with all fields
    - findBySlug("nonexistent-slug") throws NotFoundException
    - findBySlug for draft/unpublished invitation throws NotFoundException
    - findBySlug for invitation past wedding_date + 7 days returns { ...invitation, expired: true }
    - findBySlug for invitation within 7-day grace period returns normal invitation (expired: false)
    - Response includes resolved musicUrl when musicTrackId is set (look up track URL from system_music_tracks)
  </behavior>
  <action>
    1. Add `findBySlug(slug: string)` method to InvitationsService:
       - Query invitations table by slug, filter deleted_at IS NULL
       - If not found or status !== 'published', throw NotFoundException('Khong tim thay thiep cuoi')
       - Check expiry: if weddingDate exists, calculate weddingDate + 7 days (grace period) in UTC+7
       - If current date > weddingDate + 7 days, return mapRow(data) with an additional `expired: true` field
       - Otherwise return mapRow(data) with `expired: false`
       - If musicTrackId is set, look up the track URL from system_music_tracks and include as `musicUrl` in response
       - Also include `qrCodeUrl` if the invitation has one stored (add qr_code_url to SELECT_ALL and InvitationRow)

    2. Create PublicInvitationsController in a new file (NOT inside the existing @UseGuards(JwtGuard) controller):
       - @Controller('invitations') (same route prefix, different controller)
       - Single route: @Get('public/:slug') calling invitationsService.findBySlug(slug)
       - No @UseGuards -- completely unauthenticated
       - Param validation: slug is a string (no ParseUUIDPipe, slugs are not UUIDs)

    3. Register PublicInvitationsController in InvitationsModule controllers array.

    4. Add qr_code_url column awareness:
       - Add `qr_code_url: string | null` to InvitationRow interface
       - Add `qrCodeUrl: row.qr_code_url` to mapRow function
       - Add 'qr_code_url' to SELECT_ALL string
       - NOTE: The actual DB column will be added via Supabase dashboard or migration. For now, the code handles null gracefully.

    5. Update the Invitation type in packages/types/src/invitation.ts to add optional fields:
       - Add `qrCodeUrl?: string | null` to Invitation interface

    6. Convert public-invitations.spec.ts from it.todo() to real tests:
       - Mock SupabaseAdminService with chain pattern (buildSupabaseChain factory from existing tests)
       - Test findBySlug returns published invitation
       - Test findBySlug returns 404 for missing slug
       - Test findBySlug returns 404 for draft invitation
       - Test expiry logic returns expired: true after grace period
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/api && pnpm vitest run src/invitations/__tests__/public-invitations.spec.ts --reporter=verbose -x</automated>
  </verify>
  <done>GET /invitations/public/:slug works without auth, returns full invitation for published slugs, 404 for draft/missing, expired flag set correctly after grace period</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QR code generation at publish time</name>
  <files>
    apps/api/src/invitations/invitations.service.ts,
    apps/api/src/invitations/__tests__/qr-generation.spec.ts
  </files>
  <behavior>
    - generateQrCode("slug") produces a PNG buffer containing the URL {SITE_URL}/w/{slug}
    - publish() calls generateQrCode on first publish (when slug is newly generated)
    - publish() does NOT call generateQrCode on re-publish (slug already exists)
    - QR PNG is uploaded to Supabase Storage 'qr-codes' bucket at path {invitationId}/qr.png
    - qr_code_url is stored in the invitation row after upload
  </behavior>
  <action>
    1. Add `generateQrCode(invitationId: string, slug: string)` method to InvitationsService:
       - Import QRCode from 'qrcode': `import * as QRCode from 'qrcode'`
       - Build URL: `${this.configService.get('SITE_URL') ?? 'http://localhost:3000'}/w/${slug}`
       - Generate PNG buffer: `QRCode.toBuffer(url, { type: 'png', width: 512, margin: 2, errorCorrectionLevel: 'H' })`
       - Upload to Supabase Storage bucket 'qr-codes' at path `${invitationId}/qr.png` with upsert: true
       - Get public URL and update invitation row: `qr_code_url = publicUrl`
       - Wrap in try/catch -- QR generation failure should NOT block publish (log error, continue)

    2. Inject ConfigService into InvitationsService constructor (import from @nestjs/config).

    3. Wire into publish() method:
       - After successful first publish (new slug generated), call `await this.generateQrCode(id, slug)`
       - On re-publish (slug already exists), skip QR generation
       - Return the updated invitation (re-fetch after QR update to include qrCodeUrl)

    4. Create 'qr-codes' storage bucket awareness (bucket creation is via Supabase dashboard, but code references it).

    5. Convert qr-generation.spec.ts from it.todo() to real tests:
       - Mock QRCode.toBuffer to return a fake Buffer
       - Mock Supabase storage upload chain
       - Test that generateQrCode produces correct URL format
       - Test that publish calls generateQrCode on first publish
       - Test that re-publish skips QR generation
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/api && pnpm vitest run src/invitations/__tests__/qr-generation.spec.ts --reporter=verbose -x</automated>
  </verify>
  <done>QR code generated as PNG on first publish, stored to Supabase Storage, URL saved to invitation row, re-publish skips QR, tests pass</done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm vitest run --reporter=verbose` -- all tests pass including new public endpoint and QR tests
- `cd apps/api && pnpm build` -- TypeScript compiles without errors
</verification>

<success_criteria>
- GET /invitations/public/:slug returns full invitation data without authentication
- Draft/deleted/nonexistent slugs return 404
- Expired invitations return expired: true with thank-you content
- QR code PNG generated on first publish, stored in qr-codes bucket
- Re-publish preserves existing QR code
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-invitation-page/05-01-SUMMARY.md`
</output>
