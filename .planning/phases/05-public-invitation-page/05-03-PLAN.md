---
phase: 05-public-invitation-page
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - apps/web/app/w/[slug]/page.tsx
  - apps/web/app/w/[slug]/ThankYouPage.tsx
  - apps/web/app/w/[slug]/InvitationShell.tsx
  - apps/web/__tests__/components/ThankYouPage.test.tsx
  - apps/web/__tests__/components/InvitationContent.test.tsx
autonomous: true
requirements:
  - PUBL-01
  - PUBL-06
  - PUBL-09
  - PUBL-11
  - PUBL-12

must_haves:
  truths:
    - "Navigating to /w/{slug} renders the invitation without login"
    - "OG meta tags include couple names, wedding date, venue, and first photo"
    - "Expired invitation shows ThankYouPage with couple names and thank-you text"
    - "Non-existent slug shows Next.js 404 page"
    - "ISR caches the page with tag-based revalidation"
  artifacts:
    - path: "apps/web/app/w/[slug]/page.tsx"
      provides: "Server component with generateMetadata and ISR"
      exports: ["default", "generateMetadata"]
      min_lines: 40
    - path: "apps/web/app/w/[slug]/ThankYouPage.tsx"
      provides: "Post-wedding thank-you display"
      exports: ["ThankYouPage"]
    - path: "apps/web/app/w/[slug]/InvitationShell.tsx"
      provides: "Client shell that orchestrates all interactive components"
      exports: ["InvitationShell"]
  key_links:
    - from: "apps/web/app/w/[slug]/page.tsx"
      to: "apps/api/src/invitations/public-invitations.controller.ts"
      via: "fetch to /invitations/public/{slug}"
      pattern: "invitations/public"
    - from: "apps/web/app/w/[slug]/page.tsx"
      to: "apps/web/app/w/[slug]/ThankYouPage.tsx"
      via: "conditional render on expired flag"
      pattern: "expired"
    - from: "apps/web/app/w/[slug]/page.tsx"
      to: "apps/web/app/w/[slug]/InvitationShell.tsx"
      via: "default render for non-expired"
      pattern: "InvitationShell"
---

<objective>
Build the public page server component at /w/[slug] with ISR, OG metadata generation, and the thank-you page for expired invitations.

Purpose: This is the page guests visit. The server component fetches invitation data, generates OG meta tags for social sharing, and decides whether to show the full invitation or the post-wedding thank-you page. The InvitationShell is a client component placeholder that will receive interactive components (envelope, music, countdown) in later plans.
Output: Working /w/{slug} route with OG tags, ISR caching, ThankYouPage, and InvitationShell skeleton.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-invitation-page/05-CONTEXT.md
@.planning/phases/05-public-invitation-page/05-RESEARCH.md

<interfaces>
<!-- From Plan 05-01 output -->

Public API endpoint:
```typescript
GET /invitations/public/:slug
// Returns: Invitation & { expired: boolean, musicUrl?: string, qrCodeUrl?: string }
// 404 if not found or not published
```

From packages/types/src/invitation.ts:
```typescript
export interface Invitation {
  // ... all fields
  qrCodeUrl?: string | null  // Added in 05-01
}
```

From apps/web/components/templates/TemplateRenderer.tsx:
```typescript
export function TemplateRenderer({ invitation, className }: TemplateProps)
// Renders the correct template based on invitation.templateId
```

ISR revalidation (from Plan 05-02):
```typescript
// Page uses: next: { tags: [`invitation-${slug}`] } in fetch options
// Revalidation triggered by NestJS on publish/unpublish
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server component with generateMetadata and ISR</name>
  <files>
    apps/web/app/w/[slug]/page.tsx,
    apps/web/__tests__/components/InvitationContent.test.tsx
  </files>
  <behavior>
    - Page fetches invitation from /invitations/public/{slug} with ISR tag caching
    - generateMetadata returns og:title "Thiep cuoi {groomName} & {brideName}", og:image from first photo, og:description with date and venue
    - Non-existent slug calls notFound() (renders 404)
    - Expired invitation renders ThankYouPage component
    - Non-expired invitation renders InvitationShell component
  </behavior>
  <action>
    1. Create apps/web/app/w/[slug]/page.tsx as a server component (no 'use client'):
       - Define `export const revalidate = 3600` for 1-hour ISR fallback
       - Create async `getInvitation(slug: string)` helper:
         - fetch(`${API_URL}/invitations/public/${slug}`, { next: { tags: [`invitation-${slug}`] } })
         - API_URL from process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:4000'
         - If !res.ok, return null
         - Return res.json()

       - Export async `generateMetadata({ params })`:
         - Await params (Next.js 15 async params)
         - Fetch invitation via getInvitation
         - If null, return empty metadata
         - Build title: `Thiep cuoi ${invitation.groomName} & ${invitation.brideName}`
         - Build description: wedding date (Intl.DateTimeFormat vi-VN) + venue name
         - og:image: invitation.photoUrls[0] or a default fallback '/default-og.jpg'
         - Set openGraph.type = 'website', openGraph.locale = 'vi_VN'
         - OG image dimensions: { width: 1200, height: 630 }
         - Set metadataBase from process.env.NEXT_PUBLIC_SITE_URL for absolute URL composition

       - Export default async page component:
         - Await params
         - Fetch invitation via getInvitation
         - If null, call notFound()
         - If invitation.expired === true, render <ThankYouPage invitation={invitation} />
         - Otherwise render <InvitationShell invitation={invitation} />
         - Pass invitation data as serialized props (server -> client boundary)

    2. Convert InvitationContent.test.tsx stubs to real tests (test the rendering logic):
       - Mock the TemplateRenderer component
       - Test that InvitationShell receives invitation props
       - Note: Full component tests will be enhanced in later plans as interactive features are added
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && pnpm vitest run __tests__/components/InvitationContent.test.tsx --reporter=verbose -x</automated>
  </verify>
  <done>Page at /w/[slug] fetches from public API, generates OG meta, handles 404 and expired states, ISR with tag-based caching</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThankYouPage and InvitationShell skeleton</name>
  <files>
    apps/web/app/w/[slug]/ThankYouPage.tsx,
    apps/web/app/w/[slug]/InvitationShell.tsx,
    apps/web/__tests__/components/ThankYouPage.test.tsx
  </files>
  <behavior>
    - ThankYouPage shows couple names as heading
    - ThankYouPage shows thankYouText or a default Vietnamese message
    - ThankYouPage shows first photo from photoUrls if available
    - ThankYouPage does NOT show bank QR, countdown, or envelope animation
    - InvitationShell renders TemplateRenderer with invitation data (placeholder for interactive wiring)
  </behavior>
  <action>
    1. Create apps/web/app/w/[slug]/ThankYouPage.tsx ('use client'):
       - Accept props: { invitation: Invitation & { expired: boolean, musicUrl?: string } }
       - Render a warm, centered layout:
         - Heading: "{groomName} & {brideName}" in Playfair Display serif font
         - Sub-heading: "Cam on quy khach" or similar
         - thankYouText from invitation (or default: "Xin chan thanh cam on quy khach da den chung vui cung chung toi.")
         - First photo from photoUrls displayed as a rounded image (if available)
         - No bank QR section
         - No countdown timer
         - No envelope animation
         - Simple, clean design with template-appropriate colors
       - Use Tailwind classes for styling, keep it mobile-first
       - Template-aware colors: check invitation.templateId for color palette (Traditional: burgundy/gold, Modern: rose, Minimalist: gray/cream)

    2. Create apps/web/app/w/[slug]/InvitationShell.tsx ('use client'):
       - Accept props: { invitation: Invitation & { expired: boolean, musicUrl?: string } }
       - For now, render TemplateRenderer directly with invitation data
       - Add state placeholders that will be filled in later plans:
         - `const [envelopeOpened, setEnvelopeOpened] = useState(false)` -- will gate envelope vs content
         - `const [musicStarted, setMusicStarted] = useState(false)` -- will gate music playback
       - When envelopeOpened is false: show a placeholder div with invitation info (will be replaced by EnvelopeAnimation in Plan 05-04)
       - When envelopeOpened is true: render TemplateRenderer
       - Add a temporary "open" button for testing until the envelope is wired

    3. Convert ThankYouPage.test.tsx stubs to real tests:
       - Test couple names displayed
       - Test thankYouText displayed (or default when empty)
       - Test first photo rendered when available
       - Test no bank QR rendered
       - Test no countdown rendered
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && pnpm vitest run __tests__/components/ThankYouPage.test.tsx --reporter=verbose -x</automated>
  </verify>
  <done>ThankYouPage shows warm post-wedding content, InvitationShell renders with envelope state management, tests pass</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm vitest run --reporter=verbose` -- all tests pass
- Navigate to /w/test-slug in browser (with NestJS running and a published invitation) -- page renders
- View page source -- og:title and og:image present in HTML
</verification>

<success_criteria>
- /w/{slug} renders without authentication
- OG meta tags include couple photo, names, date, venue
- Expired invitations show ThankYouPage with names, text, first photo
- Non-existent slugs show 404
- ISR caches with invitation-{slug} tag
- InvitationShell renders TemplateRenderer (placeholder state for envelope wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-invitation-page/05-03-SUMMARY.md`
</output>
