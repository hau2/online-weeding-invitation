---
phase: 05-public-invitation-page
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - apps/web/app/w/[slug]/EnvelopeAnimation.tsx
  - apps/web/app/w/[slug]/FallingPetals.tsx
  - apps/web/__tests__/components/EnvelopeAnimation.test.tsx
autonomous: true
requirements:
  - PUBL-03
  - PUBL-04
  - PUBL-10

must_haves:
  truths:
    - "Envelope renders in sealed state with wax seal and template-specific colors"
    - "Tapping the envelope triggers a multi-stage reveal animation"
    - "After reveal, envelope fades and invitation content becomes visible"
    - "onOpen callback fires when animation completes"
    - "Falling petals animate continuously using pure CSS keyframes"
    - "Skip button allows bypassing the animation"
  artifacts:
    - path: "apps/web/app/w/[slug]/EnvelopeAnimation.tsx"
      provides: "Multi-stage envelope reveal with framer-motion"
      exports: ["EnvelopeAnimation"]
      min_lines: 80
    - path: "apps/web/app/w/[slug]/FallingPetals.tsx"
      provides: "CSS-only petal overlay"
      exports: ["FallingPetals"]
  key_links:
    - from: "apps/web/app/w/[slug]/EnvelopeAnimation.tsx"
      to: "framer-motion"
      via: "motion components + variants"
      pattern: "motion\\."
    - from: "apps/web/app/w/[slug]/EnvelopeAnimation.tsx"
      to: "apps/web/app/w/[slug]/InvitationShell.tsx"
      via: "onOpen callback prop"
      pattern: "onOpen"
---

<objective>
Build the envelope opening animation and falling petals effect -- the product's signature "wow" moment.

Purpose: The envelope is the differentiating experience. Guests see a realistic envelope with a wax seal; tapping it triggers a cinematic reveal. Falling petals add romantic atmosphere. These are the visual elements that make the product feel like receiving a real wedding invitation.
Output: EnvelopeAnimation component with framer-motion orchestration, FallingPetals with pure CSS keyframes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-invitation-page/05-CONTEXT.md
@.planning/phases/05-public-invitation-page/05-RESEARCH.md

<interfaces>
<!-- From Plan 05-03 output -->

From apps/web/app/w/[slug]/InvitationShell.tsx:
```typescript
// EnvelopeAnimation integrates here:
const [envelopeOpened, setEnvelopeOpened] = useState(false)
// When envelopeOpened is false: show EnvelopeAnimation
// When envelope triggers onOpen: setEnvelopeOpened(true)
```

Template color schemes (from CONTEXT.md locked decisions):
- Traditional: red/gold envelope
- Modern: white/rose gold envelope
- Minimalist: cream/ivory envelope

Props interface for EnvelopeAnimation:
```typescript
interface EnvelopeAnimationProps {
  templateId: TemplateId
  groomName: string
  brideName: string
  guestName?: string  // from ?to= param
  onOpen: () => void  // fires when reveal animation completes
}
```

Available libraries (already installed):
- framer-motion ^12.0.0 for animation orchestration
- canvas-confetti ^1.9.4 for burst effect (dynamic import)
- lucide-react for skip button icon
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EnvelopeAnimation component</name>
  <files>
    apps/web/app/w/[slug]/EnvelopeAnimation.tsx,
    apps/web/__tests__/components/EnvelopeAnimation.test.tsx
  </files>
  <behavior>
    - Renders sealed envelope with wax seal (double-happiness symbol or heart)
    - Shows "Tran trong kinh moi" text on envelope front
    - Shows guest name below greeting when provided
    - Template-specific colors: Traditional (red body, gold trim), Modern (white body, rose gold trim), Minimalist (cream body, ivory/gray trim)
    - On tap/click: flap lifts (rotateX), card slides up, envelope fades, calls onOpen
    - Skip button visible for accessibility -- clicking it calls onOpen immediately
    - Confetti burst fires on reveal (dynamic import canvas-confetti)
  </behavior>
  <action>
    1. Create apps/web/app/w/[slug]/EnvelopeAnimation.tsx ('use client'):
       - Props: { templateId: TemplateId, groomName: string, brideName: string, guestName?: string, onOpen: () => void }
       - Use framer-motion's motion.div with variants for orchestrated animation

       Envelope structure (layers, bottom to top):
       a. Envelope body: rectangular div with subtle paper texture (CSS gradient), rounded corners
       b. Envelope back flap: triangular shape at top (CSS clip-path or borders), rotateX animation
       c. Wax seal: circular div centered on the flap seam with "囍" character or a heart
       d. Greeting text: "Tran trong kinh moi" + optional guest name below
       e. Skip button: small "Bo qua >>" text link in bottom-right corner

       Template color config:
       ```typescript
       const ENVELOPE_COLORS: Record<TemplateId, { body: string, flap: string, seal: string, text: string }> = {
         traditional: { body: '#8B0000', flap: '#A52A2A', seal: '#DAA520', text: '#FFD700' },
         modern: { body: '#FFFFFF', flap: '#F5F5F5', seal: '#B76E79', text: '#333333' },
         minimalist: { body: '#FFFDD0', flap: '#F5F5DC', seal: '#888888', text: '#333333' },
       }
       ```

       Animation sequence (framer-motion variants):
       - State "sealed": envelope centered on screen, full opacity, scale 1
       - On tap: transition to "opening" state
         - Flap rotateX: 0 -> -180deg over 0.8s (flap lifts open, CSS perspective for 3D)
         - Wax seal opacity: 1 -> 0 over 0.3s (disappears as flap opens)
         - After 0.5s delay: card slides up from inside envelope (y: 0 -> -100px)
       - After card slides: transition to "revealed" state
         - Entire envelope scales down (0.8) and fades out (opacity 0) over 0.5s
         - Fire canvas-confetti burst:
           ```typescript
           const confettiModule = await import('canvas-confetti')
           const confetti = confettiModule.default ?? confettiModule
           confetti({ particleCount: 80, spread: 60, origin: { y: 0.6 } })
           ```
         - Call onOpen() when fade completes

       Skip button:
       - Position: absolute, bottom-right of envelope container
       - Text: "Bo qua >>" in small font
       - onClick: immediately calls onOpen() (no animation)
       - Style: subtle, semi-transparent, accessible (min 44px tap target)

       Performance gate (per CONTEXT.md decision):
       - Measure frame time during animation
       - If any frame exceeds 20ms, halve particle count in confetti
       - Use `requestAnimationFrame` timestamp delta for measurement

    2. Convert EnvelopeAnimation.test.tsx stubs to real tests:
       - Use @testing-library/react render + screen
       - Test: sealed envelope renders with greeting text
       - Test: guest name appears when provided
       - Test: skip button is present and accessible
       - Test: clicking envelope triggers animation state change (mock framer-motion)
       - Test: template colors applied correctly (check style attributes or class names)
       - Mock framer-motion: vi.mock('framer-motion', ...) to return basic motion.div components
       - Mock canvas-confetti: vi.mock('canvas-confetti')
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && pnpm vitest run __tests__/components/EnvelopeAnimation.test.tsx --reporter=verbose -x</automated>
  </verify>
  <done>EnvelopeAnimation renders sealed state, shows greeting text and guest name, tap triggers multi-stage reveal, confetti fires, skip button works, template colors applied, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: FallingPetals CSS-only overlay</name>
  <files>apps/web/app/w/[slug]/FallingPetals.tsx</files>
  <action>
    1. Create apps/web/app/w/[slug]/FallingPetals.tsx ('use client'):
       - Props: { templateId: TemplateId, enabled?: boolean }
       - Renders a fixed-position overlay container with pointer-events: none
       - Contains 15-20 petal div elements with randomized animation-delay and horizontal positions
       - enabled defaults to true; when false, renders null (used to disable during envelope)

       Petal styling (pure CSS, no JavaScript animation loop per CONTEXT.md decision):
       - Each petal: small elliptical div with radial-gradient
       - CSS @keyframes petal-fall: translateY from -10vh to 110vh with gentle horizontal sway (translateX oscillation) and rotation
       - animation: petal-fall linear infinite, duration 8-15s (randomized per petal)
       - animation-delay: staggered 0-10s per petal for natural distribution
       - z-index: 1 (above content, below floating buttons)

       Template-specific petal colors:
       - Traditional: pink/red petals (cherry blossom)
       - Modern: soft pink/white petals (rose petals)
       - Minimalist: subtle cream/beige petals (barely visible, atmospheric)

       Generate petals via Array.from({ length: 18 }).map((_, i) => ...)
       Each petal gets randomized:
       - left position: `${(i * 5.5 + Math.random() * 5)}%`
       - animation-duration: `${8 + Math.random() * 7}s`
       - animation-delay: `${Math.random() * 10}s`
       - size variation: width 8-16px, height 10-20px

    2. Use inline styles for the animations (not a separate CSS file) since values are dynamic per-petal.
       Define the @keyframes via a <style> tag or Tailwind arbitrary values.
       Alternatively, define the keyframes in globals.css as a reusable animation.

    3. Performance note: CSS keyframes are GPU-accelerated and don't block the main thread.
       Using transform and opacity only (no layout-triggering properties).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && pnpm vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>FallingPetals renders 15-20 CSS-animated petal elements with template-specific colors, pure CSS animation (no JS loop), pointer-events: none overlay</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm vitest run --reporter=verbose` -- all tests pass
- EnvelopeAnimation visually renders (manual check in browser)
- Petals fall smoothly without JS overhead
</verification>

<success_criteria>
- Envelope renders with template-specific colors and wax seal
- Tap triggers flap-open -> card-slide -> fade sequence
- Confetti burst fires on reveal
- onOpen callback fires after animation or skip
- Petals animate via CSS keyframes only (no requestAnimationFrame loop)
- Skip button accessible with 44px+ tap target
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-invitation-page/05-04-SUMMARY.md`
</output>
