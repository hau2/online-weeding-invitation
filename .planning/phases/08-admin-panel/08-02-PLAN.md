---
phase: 08-admin-panel
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/app/(admin)/admin/page.tsx
  - apps/web/components/admin/AdminDashboardCharts.tsx
  - apps/web/app/(admin)/admin/nguoi-dung/page.tsx
  - apps/web/components/admin/UserDetailDialog.tsx
autonomous: true
requirements:
  - ADMN-02
  - ADMN-03

must_haves:
  truths:
    - "Admin dashboard shows real stat cards with live data (total users, invitations, premium, published counts)"
    - "Admin dashboard shows a recharts line chart of invitations created over the last 30 days"
    - "Admin can search users by email, filter by active/locked status"
    - "Admin can lock/unlock a user account from the user list"
    - "Admin can hard-delete a user (with confirmation dialog)"
    - "Admin can change a user's role between user and admin"
    - "Admin can view a user's invitations in a detail dialog"
  artifacts:
    - path: "apps/web/app/(admin)/admin/page.tsx"
      provides: "Dashboard with real stats"
      min_lines: 40
    - path: "apps/web/app/(admin)/admin/nguoi-dung/page.tsx"
      provides: "Users management page"
      min_lines: 80
    - path: "apps/web/components/admin/UserDetailDialog.tsx"
      provides: "User detail with invitations list"
      min_lines: 40
  key_links:
    - from: "apps/web/app/(admin)/admin/page.tsx"
      to: "/admin/stats"
      via: "apiFetch in useEffect"
      pattern: "apiFetch.*admin/stats"
    - from: "apps/web/app/(admin)/admin/nguoi-dung/page.tsx"
      to: "/admin/users"
      via: "apiFetch with search/filter params"
      pattern: "apiFetch.*admin/users"
---

<objective>
Wire the admin dashboard to real data and build the users management page. Dashboard replaces Phase 2 placeholder stats with live numbers from the admin stats API. Users page provides full CRUD: search, filter, lock/unlock, delete, role change, and view user invitations.

Purpose: These are the two most-used admin pages -- dashboard for at-a-glance health, users for day-to-day management.
Output: Working dashboard with stats + chart, full users management page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-panel/08-01-SUMMARY.md

@apps/web/app/(admin)/admin/page.tsx
@apps/web/components/admin/StatCard.tsx
@apps/web/components/admin/AdminDashboardCharts.tsx
@apps/web/components/admin/AdminSidebar.tsx
@apps/web/lib/api.ts

<interfaces>
<!-- From Plan 08-01 shared types (packages/types/src/admin.ts) -->
```typescript
export interface AdminStats {
  totalUsers: number
  totalInvitations: number
  publishedInvitations: number
  premiumInvitations: number
  chartData: { date: string; count: number }[]
}

export interface AdminUser {
  id: string
  email: string
  role: string
  isLocked: boolean
  invitationCount: number
  createdAt: string
  updatedAt: string
}
```

<!-- From existing apps/web/components/admin/StatCard.tsx -->
```typescript
interface StatCardProps {
  title: string; value: string | number; description?: string;
  icon: LucideIcon; trend?: { value: number; label: string }; className?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard overview page with real data + chart</name>
  <files>
    apps/web/app/(admin)/admin/page.tsx,
    apps/web/components/admin/AdminDashboardCharts.tsx
  </files>
  <action>
Rewrite the admin dashboard page to fetch real data:

**page.tsx** -- Convert to `'use client'`. On mount, call `apiFetch<AdminStats>('/admin/stats', { credentials: 'include' })`. Replace PLACEHOLDER_STATS with real data:
- "Tong nguoi dung" card: `stats.totalUsers` with Users icon
- "Tong thiep cuoi" card: `stats.totalInvitations` with FileText icon
- "Da xuat ban" card: `stats.publishedInvitations` with CheckCircle icon
- "Premium" card: `stats.premiumInvitations` with DollarSign icon

Show a Loader2 spinner while loading. If error, show error toast and em-dash fallback values.

Remove the Phase 2 placeholder description text ("Du lieu thuc te se duoc ket noi tai Phase 8").

**AdminDashboardCharts.tsx** -- Accept `chartData` as prop (array of `{ date: string; count: number }`). Replace PLACEHOLDER_DATA with real chart data. Use `LineChart` (not BarChart) per user decision: "1 recharts line chart (30 days)". X-axis shows date labels formatted as dd/MM. Y-axis shows count. Line color: `#6b7280` (gray-500). Remove the revenue placeholder chart (second grid column) -- just show the single invitations chart full-width.

Remove the Phase 2 placeholder text ("Du lieu thuc te se co o Phase 8").
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard loads real stats from /admin/stats endpoint. Four stat cards show live numbers. Line chart renders 30-day invitation creation trend. No placeholder text remains.</done>
</task>

<task type="auto">
  <name>Task 2: Users management page with search, filter, and actions</name>
  <files>
    apps/web/app/(admin)/admin/nguoi-dung/page.tsx,
    apps/web/components/admin/UserDetailDialog.tsx
  </files>
  <action>
Create the users management page at `/admin/nguoi-dung`:

**page.tsx** (`'use client'`):
- Page header: "Nguoi dung" with Users icon
- Search input (debounced 300ms) filtering by email
- Status filter tabs or select: "Tat ca" / "Hoat dong" / "Da khoa"
- Results table/list showing: email, role badge (admin=purple, user=gray), status badge (locked=red, active=green), invitation count, created date (vi-VN format), actions
- Pagination: simple "Trang truoc / Trang sau" buttons with page number display. Fetch with `?page=N&limit=20&search=X&status=Y`.
- Action buttons per user row:
  - **Lock/Unlock** toggle button (Lock icon for active users, Unlock icon for locked users). Calls `POST /admin/users/:id/lock` or `/unlock`. Toast on success. Refresh list.
  - **Xem chi tiet** button opens UserDetailDialog
  - **Xoa** button (Trash icon, red) with confirmation dialog: "Ban co chac muon xoa vinh vien nguoi dung nay? Tat ca thiep cuoi va hinh anh se bi xoa." Calls `DELETE /admin/users/:id`. Toast on success.
  - **Doi vai tro** dropdown or button: "Chuyen thanh admin" / "Chuyen thanh user". Calls `POST /admin/users/:id/role` with body `{ role: 'admin' | 'user' }`.

Empty state: "Khong tim thay nguoi dung nao" with Users icon.

**UserDetailDialog.tsx**:
- Dialog showing user details: email, role, status, created date
- Below: list of user's invitations (fetched via `GET /admin/users/:id` which returns user + invitations). Show each invitation with: groom & bride names, status badge, plan badge, slug link, created date.
- Close button.

UI patterns: Use existing shadcn/ui Button, Dialog components. Style consistent with admin payments page (white cards, rounded-xl borders, gray-200 borders). Use Loader2 for loading states. Toast via sonner for all actions.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Users page shows paginated, searchable, filterable user list. Lock/unlock, delete, and role change actions work with toast feedback. UserDetailDialog shows user info and their invitations.</done>
</task>

</tasks>

<verification>
- Navigate to /admin and see real stat cards (not em-dash placeholders)
- Navigate to /admin/nguoi-dung and see user list with search/filter
- Lock a user, see status change to "Da khoa"
- View user detail dialog showing their invitations
- TypeScript compiles cleanly
</verification>

<success_criteria>
- Dashboard stat cards show real numbers from API
- 30-day line chart renders with actual data points
- Users page supports search by email, filter by status
- Lock/unlock, delete, role change actions work end-to-end
- UserDetailDialog displays user info + invitations list
- All pages use Vietnamese labels
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-panel/08-02-SUMMARY.md`
</output>
