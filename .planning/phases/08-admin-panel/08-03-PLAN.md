---
phase: 08-admin-panel
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/app/(admin)/admin/thiep-cuoi/page.tsx
  - apps/web/app/(admin)/admin/nhac/page.tsx
autonomous: true
requirements:
  - ADMN-04
  - ADMN-06
  - ADMN-07

must_haves:
  truths:
    - "Admin can list all invitations with search by couple names or slug"
    - "Admin can filter invitations by status and plan"
    - "Admin can view any invitation in read-only mode (no edit controls)"
    - "Admin can disable an invitation for violations -- disabled invitations show 404 on public page"
    - "Admin can re-enable a previously disabled invitation"
    - "Admin can see public link for each published invitation"
    - "Admin can list all music tracks including inactive ones with usage counts"
    - "Admin can upload a new MP3 file to the system music library"
    - "Admin can enable/disable a music track -- disabled tracks are hidden from editor but still play on existing invitations"
    - "Admin can delete a music track only if its usage count is 0"
  artifacts:
    - path: "apps/web/app/(admin)/admin/thiep-cuoi/page.tsx"
      provides: "Invitations management page"
      min_lines: 80
    - path: "apps/web/app/(admin)/admin/nhac/page.tsx"
      provides: "Music library management page"
      min_lines: 60
  key_links:
    - from: "apps/web/app/(admin)/admin/thiep-cuoi/page.tsx"
      to: "/admin/invitations"
      via: "apiFetch with search/filter params"
      pattern: "apiFetch.*admin/invitations"
    - from: "apps/web/app/(admin)/admin/nhac/page.tsx"
      to: "/admin/music-tracks"
      via: "apiFetch and apiUpload"
      pattern: "apiFetch.*admin/music"
---

<objective>
Build the invitations oversight page and music library management page. Admin can search/filter/disable/enable invitations without editing content, and manage the system music library with upload, toggle, and delete capabilities.

Purpose: Invitations oversight for content moderation; music library for curating the track selection available to users.
Output: Two fully functional admin pages at /admin/thiep-cuoi and /admin/nhac.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-panel/08-01-SUMMARY.md

@apps/web/lib/api.ts
@apps/web/components/admin/AdminSidebar.tsx

<interfaces>
<!-- From Plan 08-01 shared types -->
```typescript
export interface AdminInvitation {
  id: string
  userId: string
  userEmail: string
  slug: string | null
  status: string
  templateId: string
  groomName: string
  brideName: string
  plan: string
  paymentStatus: string
  isDisabled: boolean
  createdAt: string
  updatedAt: string
}

export interface AdminMusicTrack {
  id: string
  title: string
  artist: string
  url: string
  duration: number
  isActive: boolean
  usageCount: number
  sortOrder: number
  createdAt: string
  updatedAt: string
}
```

<!-- From apps/web/lib/api.ts -->
```typescript
export async function apiFetch<T>(path: string, options?: FetchOptions): Promise<{ data: T | null; error: string | null }>
export async function apiUpload<T>(path: string, formData: FormData): Promise<{ data: T | null; error: string | null }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invitations management page</name>
  <files>apps/web/app/(admin)/admin/thiep-cuoi/page.tsx</files>
  <action>
Create the invitations management page at `/admin/thiep-cuoi` (`'use client'`):

**Page header**: "Thiep cuoi" with FileText icon.

**Search and filters**:
- Search input (debounced 300ms): searches by groom_name, bride_name, or slug. Placeholder: "Tim theo ten hoac slug..."
- Status filter: select/tabs for "Tat ca", "Nhap", "Da xuat ban", "Save the Date", "Het han"
- Plan filter: select for "Tat ca", "Mien phi", "Premium"

**Invitation list**: Fetch from `GET /admin/invitations?search=X&status=Y&plan=Z&page=N&limit=20`.
Each row shows:
- Couple names: "{groomName} & {brideName}"
- Status badge: Nhap (gray), Da xuat ban (green), Save the Date (blue), Het han (red)
- Plan badge: Free (gray), Premium (amber)
- Disabled badge: if isDisabled, show red "Da vo hieu hoa" badge
- User email (truncated, gray text)
- Slug (monospace, if set)
- Created date (vi-VN format)
- Actions column:
  - **Xem** link: if slug exists and not disabled, link to `/w/{slug}` (opens in new tab). Use ExternalLink icon.
  - **Vo hieu hoa / Kich hoat** toggle: calls `POST /admin/invitations/:id/disable` or `/enable`. Show Shield icon for disable, ShieldCheck for enable. Toast on success. Refresh list.

**Pagination**: Same pattern as users page -- "Trang truoc / Trang sau" with page count.

**Empty state**: "Khong tim thay thiep cuoi nao" with FileText icon.

**Read-only view**: When clicking an invitation row (not the action buttons), expand it or open a dialog showing full invitation details: all fields (groom/bride names, parents, ceremony dates, venues, love story, message, thank you text, template, photos count, music track, bank info, plan, payment status, admin notes). All fields are display-only -- no edit inputs. Show a "Dong" close button.

Style: Consistent with admin payments page. White cards, rounded-xl, gray-200 borders. Use Loader2 for loading.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Invitations page lists all invitations with search/filter by status and plan. Disable/enable toggle works. Read-only detail view shows full invitation data. Public links open in new tab. No edit controls exposed.</done>
</task>

<task type="auto">
  <name>Task 2: Music library management page</name>
  <files>apps/web/app/(admin)/admin/nhac/page.tsx</files>
  <action>
Create the music library management page at `/admin/nhac` (`'use client'`):

**Page header**: "Thu vien nhac" with Music icon.

**Upload section** at top:
- File input accepting `.mp3` files only. Max 10MB.
- Title input (required) and Artist input (optional).
- Upload button with Loader2 spinner during upload.
- Uses `apiUpload` to `POST /admin/music-tracks` with FormData containing the file + title + artist fields.
- On success: toast "Da tai len thanh cong", refresh track list.
- On error: toast with error message.

**Track list**: Fetch from `GET /admin/music-tracks` (returns all tracks including inactive).
Each track card shows:
- Title (bold) and Artist (gray, if set)
- Duration formatted as M:SS
- Usage count badge: "{N} thiep dang su dung"
- Active status: green dot for active, red dot for inactive
- Actions:
  - **Toggle** button: if active, show "Vo hieu hoa" (EyeOff icon); if inactive, show "Kich hoat" (Eye icon). Calls `POST /admin/music-tracks/:id/toggle`. Toast on success.
  - **Xoa** button (Trash icon, red): only enabled if usageCount === 0. If usageCount > 0, button is disabled with tooltip "Khong the xoa -- dang duoc su dung boi {N} thiep". Calls `DELETE /admin/music-tracks/:id`. Confirmation dialog: "Ban co chac muon xoa bai hat nay?". Toast on success.

**Empty state**: "Chua co bai hat nao trong thu vien" with Music icon.

**ADMN-07 contract note**: Add a subtle info text below the track list: "Luu y: Bai hat da vo hieu hoa van phat tren cac thiep dang su dung. Chi an khoi trinh chon nhac." This reminds admin of the inactive-still-plays contract.

Style: Upload section has a dashed border drop zone appearance. Track cards in a vertical list with white backgrounds.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Music library page lists all tracks with usage counts. Upload form creates new tracks via API. Toggle active/inactive works. Delete only allowed for tracks with 0 usage. ADMN-07 contract documented in UI.</done>
</task>

</tasks>

<verification>
- Navigate to /admin/thiep-cuoi, see invitation list with filters
- Disable an invitation, verify public page returns 404
- Enable it back, verify public page works again
- Navigate to /admin/nhac, see track list with usage counts
- Upload a new MP3, see it appear in the list
- Toggle a track inactive, verify it no longer appears in editor music picker
- TypeScript compiles cleanly
</verification>

<success_criteria>
- Invitations page supports search/filter by couple names, slug, status, plan
- Disable/enable toggle changes invitation visibility on public page
- Read-only detail view shows all invitation fields without edit capability
- Music library shows all tracks with usage counts
- Upload creates new track with title/artist metadata
- Toggle changes track visibility in editor picker
- Delete blocked for tracks with usage > 0
- ADMN-07 contract (inactive still plays) enforced and documented
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-panel/08-03-SUMMARY.md`
</output>
