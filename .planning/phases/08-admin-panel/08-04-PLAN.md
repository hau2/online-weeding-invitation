---
phase: 08-admin-panel
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/app/(admin)/admin/giao-dien/page.tsx
  - apps/web/app/(admin)/admin/goi-dich-vu/page.tsx
  - apps/web/app/(admin)/admin/cai-dat/page.tsx
  - apps/web/app/(admin)/admin/thanh-toan/page.tsx
autonomous: true
requirements:
  - ADMN-05
  - ADMN-08
  - ADMN-09
  - ADMN-10

must_haves:
  truths:
    - "Admin can view all 3 themes with name, tag, thumbnail, and active status"
    - "Admin can enable/disable a theme -- disabled themes are hidden from template selector"
    - "Admin can edit theme metadata: name, tag, and thumbnail (per ADMN-05 and CONTEXT.md locked decision)"
    - "Admin can view service plan configuration (free vs premium limits)"
    - "Admin can update service plan limits (photo count, template access)"
    - "Admin can mark a payment transaction as refunded with notes"
    - "Admin can add internal notes to any payment transaction"
    - "Admin can export payment history to CSV"
    - "Admin can configure admin bank QR image, bank name, account holder"
    - "Admin can configure watermark text and opacity"
    - "Admin can configure invitation expiry grace period"
    - "Admin can configure upload limits (max photo size, max photos)"
  artifacts:
    - path: "apps/web/app/(admin)/admin/giao-dien/page.tsx"
      provides: "Themes management page with metadata editing"
      min_lines: 50
    - path: "apps/web/app/(admin)/admin/goi-dich-vu/page.tsx"
      provides: "Service plans configuration page"
      min_lines: 40
    - path: "apps/web/app/(admin)/admin/cai-dat/page.tsx"
      provides: "System settings page"
      min_lines: 80
    - path: "apps/web/app/(admin)/admin/thanh-toan/page.tsx"
      provides: "Enhanced payments page with refund + notes + CSV"
      min_lines: 100
  key_links:
    - from: "apps/web/app/(admin)/admin/cai-dat/page.tsx"
      to: "/admin/system-settings"
      via: "apiFetch GET and PUT"
      pattern: "apiFetch.*admin/system-settings"
    - from: "apps/web/app/(admin)/admin/thanh-toan/page.tsx"
      to: "/invitations/admin"
      via: "apiFetch for upgrade operations"
      pattern: "apiFetch.*invitations/admin"
    - from: "apps/web/app/(admin)/admin/giao-dien/page.tsx"
      to: "/admin/themes"
      via: "apiFetch GET, POST toggle, PUT metadata"
      pattern: "apiFetch.*admin/themes"
    - from: "apps/web/app/(admin)/admin/goi-dich-vu/page.tsx"
      to: "/admin/system-settings"
      via: "apiFetch GET and PUT for uploadLimits"
      pattern: "apiFetch.*admin/system-settings"
---

<objective>
Build the remaining four admin pages: themes management (with metadata editing), service plans configuration, system settings, and enhanced payments. These complete the admin panel with full operational control over platform configuration and billing.

Purpose: Themes and service plans let admin control the product offering. System settings centralizes configuration that was previously hardcoded or in env vars. Payments enhancement adds refund/notes/export for financial tracking.
Output: Four admin pages completing the Phase 8 admin panel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-panel/08-01-SUMMARY.md

@apps/web/app/(admin)/admin/thanh-toan/page.tsx
@apps/web/lib/api.ts
@apps/web/components/admin/AdminSidebar.tsx

<interfaces>
<!-- From Plan 08-01 shared types -->
```typescript
export interface SystemSettings {
  paymentConfig: {
    bankQrUrl: string | null
    bankName: string
    bankAccountHolder: string
    pricePerInvitation: number
  }
  watermarkConfig: { text: string; opacity: number }
  expiryConfig: { gracePeriodDays: number }
  uploadLimits: {
    maxPhotoSizeMb: number
    maxPhotosPerInvitation: number
    maxPhotosPremium: number
  }
}

export interface ThemeInfo {
  id: string; name: string; tag: string; thumbnail: string | null; isActive: boolean;
}
```

<!-- Existing Invitation type with new fields from 08-01 -->
```typescript
export interface Invitation {
  /* ... existing fields ... */
  adminNotes?: string
  isDisabled?: boolean
}
export type PaymentStatus = 'none' | 'pending' | 'rejected' | 'refunded'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Themes page (with metadata editing) + Service plans page</name>
  <files>
    apps/web/app/(admin)/admin/giao-dien/page.tsx,
    apps/web/app/(admin)/admin/goi-dich-vu/page.tsx
  </files>
  <action>
**Themes page** (`/admin/giao-dien`, `'use client'`):

Page header: "Giao dien" with Palette icon.

Fetch themes from `GET /admin/themes`. Display as 3 cards (one per template):
- Card shows: template name (editable), tag (editable), thumbnail image (if set, otherwise a colored placeholder div with the template name), active status dot (green/red).
- Toggle button per card: "Vo hieu hoa" / "Kich hoat". Calls `POST /admin/themes/:id/toggle`. Toast on success.

**Inline metadata editing** (per ADMN-05 and CONTEXT.md locked decision: "Edit name/tag/thumbnail -- no code upload, just metadata management"):
- Each card has an "Edit" button (Pencil icon) that reveals inline edit fields:
  - Name: text input, pre-filled with current name
  - Tag: text input, pre-filled with current tag
  - Thumbnail: file input accepting image files (jpg/png/webp). Shows current thumbnail preview if exists. Upload new thumbnail via `PUT /admin/themes/:id` with FormData (if file selected) or JSON body (if only name/tag changed).
- "Luu" (Save) button calls `PUT /admin/themes/:id` with updated `{ name, tag }` and optional thumbnail file. Toast: "Da cap nhat giao dien".
- "Huy" (Cancel) button reverts to display mode.

Info text: "Giao dien da vo hieu hoa se khong hien thi trong trinh chon mau cho nguoi dung."

Layout: 3-column grid on desktop, 1-column on mobile. Cards are white with rounded-xl borders.

**Service plans page** (`/admin/goi-dich-vu`, `'use client'`):

Page header: "Goi dich vu" with CreditCard icon.

This is a simplified configuration page showing the current free vs premium tier limits. Fetch from `GET /admin/system-settings` and display the `uploadLimits` section.

Display as two side-by-side cards:

**Free tier card** (gray border):
- "Mien phi" heading
- Photo limit: shows `uploadLimits.maxPhotosPerInvitation` (e.g., "Toi da 10 anh")
- All templates: "Co" (all templates available on free)
- Watermark: "Co" (watermark applied)
- Published invitations: "Toi da 1"

**Premium tier card** (amber border):
- "Premium" heading
- Photo limit: shows `uploadLimits.maxPhotosPremium` (e.g., "Toi da 20 anh")
- All templates: "Co"
- Watermark: "Khong"
- Published invitations: "Khong gioi han"

Below the cards: an edit form to update the numeric limits. Inputs for:
- `maxPhotosPerInvitation` (label: "So anh toi da (Mien phi)")
- `maxPhotosPremium` (label: "So anh toi da (Premium)")
- `maxPhotoSizeMb` (label: "Kich thuoc anh toi da (MB)")

Save button calls `PUT /admin/system-settings` with the updated uploadLimits. Toast: "Da luu cau hinh".

Note: Per user decision, service plans are "simplified -- per-invitation free/premium already built in Phase 7, this section configures limits." So this page is just the limit config, not a full plan editor.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Themes page shows 3 template cards with enable/disable toggles AND inline metadata editing (name, tag, thumbnail). Service plans page shows free vs premium comparison and editable upload limits. Both compile and render correctly.</done>
</task>

<task type="auto">
  <name>Task 2: System settings page</name>
  <files>apps/web/app/(admin)/admin/cai-dat/page.tsx</files>
  <action>
Create the system settings page at `/admin/cai-dat` (`'use client'`):

Page header: "Cai dat he thong" with Settings icon.

Fetch current settings from `GET /admin/system-settings`. Display as form sections in white cards:

**Section 1: Thanh toan** (Payment config):
- Bank QR image: show current image if bankQrUrl exists, with a file input to upload new one. Upload via `POST /admin/system-settings/bank-qr` using apiUpload with FormData. Show preview after upload.
- Bank name input: text, value from `paymentConfig.bankName`
- Account holder input: text, value from `paymentConfig.bankAccountHolder`
- Price per invitation input: number, value from `paymentConfig.pricePerInvitation`. Label: "Gia moi thiep (VND)"
- Note: "Thong tin nay se hien thi tren trang nang cap cua nguoi dung. Thay the cac bien moi truong NEXT_PUBLIC_ADMIN_BANK_*."

**Section 2: Watermark**:
- Watermark text input: text, value from `watermarkConfig.text`. Default: "ThiepCuoiOnline.vn"
- Opacity slider or number input: range 0.05-0.5, step 0.05, value from `watermarkConfig.opacity`. Label: "Do mo (opacity)"

**Section 3: Het han thiep** (Invitation expiry):
- Grace period input: number, value from `expiryConfig.gracePeriodDays`. Label: "So ngay gia han sau ngay cuoi (ngay)"
- Help text: "Sau ngay cuoi + so ngay gia han, thiep se chuyen sang trang cam on."

**Section 4: Gioi han tai len** (Upload limits):
- Max photo file size (MB): number input
- Max photos per invitation (free): number input
- Max photos per invitation (premium): number input

**Save button**: "Luu cai dat" -- collects all form values into a SystemSettings object, calls `PUT /admin/system-settings`. Toast: "Da luu cai dat he thong". Show Loader2 during save.

Form state: use useState for each field, initialized from fetched settings. Show Loader2 while initial fetch is loading.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>System settings page loads current config from API. Bank QR upload works. All settings fields editable and saveable. Changes persist on page reload.</done>
</task>

<task type="auto">
  <name>Task 3: Enhance payments page with refund marking, notes, and CSV export</name>
  <files>apps/web/app/(admin)/admin/thanh-toan/page.tsx</files>
  <action>
Enhance the existing payments page at `/admin/thanh-toan`:

Keep the existing pending upgrades section and history section. Add the following:

**Refund marking**: In the history section, for invitations with plan='premium', add a "Hoan tien" button (amber, DollarSign icon). Calls `POST /invitations/admin/:id/mark-refund`. On success, toast "Da danh dau hoan tien", refresh history. Show "Da hoan tien" badge in orange for refunded items.

**Internal notes**: Add a small expandable notes section per history item. Show current adminNotes text (if any). Add a text input + "Luu ghi chu" button. Calls `PATCH /invitations/admin/:id/notes` with `{ notes: string }`. Toast "Da luu ghi chu". Notes are admin-internal, not visible to users.

**CSV export**: Add an "Xuat CSV" button (Download icon) at the top of the history section. When clicked, generate a CSV file client-side from the history data with columns: ID, Chu re, Co dau, Slug, Goi, Trang thai thanh toan, Ngay cap nhat, Ghi chu. Use `URL.createObjectURL` + `<a>` click pattern to download. File name: `thanh-toan-{YYYY-MM-DD}.csv`.

**Payment status display enhancement**: Show timeline-like status for each history item:
- `pending` -> "Dang cho" (yellow Clock icon)
- `none` (with plan=premium) -> "Da duyet" (green Check icon)
- `rejected` -> "Tu choi" (red X icon)
- `refunded` -> "Da hoan tien" (orange DollarSign icon)

Update the history fetch to also include refunded items. The existing `adminListUpgradeHistory` query uses `.or('plan.eq.premium,payment_status.eq.rejected')` -- update it in the API (Plan 01's admin service or invitations service) to also include `payment_status.eq.refunded`. If the API query was already updated in Plan 01, just consume it. If not, the executor should update the `adminListUpgradeHistory` method to include refunded: `.or('plan.eq.premium,payment_status.eq.rejected,payment_status.eq.refunded')`.

Style: Keep existing card-based design. Notes section uses a collapsible/expandable pattern (click to show/hide). CSV button is outlined, placed next to "Lich su" heading.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Payments page shows refund button for premium invitations. Notes input saves admin-internal notes. CSV export downloads history file. Refunded items appear in history with proper badge. All existing approve/reject/revoke functionality preserved.</done>
</task>

</tasks>

<verification>
- Navigate to /admin/giao-dien, see 3 theme cards with toggle AND edit button for name/tag/thumbnail
- Edit a theme name, save, reload and verify the name persisted
- Navigate to /admin/goi-dich-vu, see free vs premium comparison
- Navigate to /admin/cai-dat, update bank QR and watermark text, save, reload and verify persisted
- Navigate to /admin/thanh-toan, verify refund button, notes, and CSV export
- TypeScript compiles cleanly for all new pages
</verification>

<success_criteria>
- Themes page allows toggling template visibility AND editing name/tag/thumbnail metadata
- Service plans page shows tier comparison and editable limits
- System settings page persists all config (payment, watermark, expiry, upload limits)
- Bank QR upload works in system settings
- Payments page has refund marking, notes, and CSV export
- All 8 admin sidebar links lead to working pages
- All pages use Vietnamese labels consistently
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-panel/08-04-SUMMARY.md`
</output>
