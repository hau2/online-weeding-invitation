---
phase: 09-polish-and-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/w/[slug]/EnvelopeAnimation.tsx
  - apps/web/app/w/[slug]/EnvelopeAnimationFallback.tsx
  - apps/web/app/w/[slug]/MusicPlayer.tsx
  - apps/web/components/templates/TemplateTraditional.tsx
  - apps/web/components/templates/TemplateModern.tsx
  - apps/web/components/templates/TemplateMinimalist.tsx
autonomous: true
requirements:
  - PUBL-10

must_haves:
  truths:
    - "On low-end devices (>20ms avg frame time), envelope animation gracefully falls back to simple fade reveal without framer-motion"
    - "Music play/pause button is 56px with clearly visible equalizer bars for elderly users"
    - "Bank QR images display at 240px+ with Vietnamese instruction text for elderly guests"
    - "All interactive touch targets on public page are at least 48px"
    - "Skip button on envelope is always visible, centered at bottom, with larger text"
  artifacts:
    - path: "apps/web/app/w/[slug]/EnvelopeAnimationFallback.tsx"
      provides: "CSS-only fade fallback animation component"
      min_lines: 30
    - path: "apps/web/app/w/[slug]/EnvelopeAnimation.tsx"
      provides: "Performance gate measuring frame times before animation"
      contains: "useFallback"
    - path: "apps/web/app/w/[slug]/MusicPlayer.tsx"
      provides: "56px floating music button with larger equalizer bars"
      contains: "h-14 w-14"
    - path: "apps/web/components/templates/TemplateTraditional.tsx"
      provides: "Bank QR at 240px+ with instruction text"
      contains: "max-w-[240px]"
  key_links:
    - from: "apps/web/app/w/[slug]/EnvelopeAnimation.tsx"
      to: "apps/web/app/w/[slug]/EnvelopeAnimationFallback.tsx"
      via: "conditional render based on useFallback state"
      pattern: "useFallback.*EnvelopeAnimationFallback"
---

<objective>
Add performance gate with CSS-only fallback to envelope animation, and apply elderly-friendly UX improvements to the music player and bank QR display across all templates.

Purpose: Ensure smooth animation on low-end Android devices with graceful degradation, and make the public invitation page accessible for elderly Vietnamese guests (grandparents) who need larger touch targets and clearer visual cues.

Output: EnvelopeAnimationFallback component, performance-gated EnvelopeAnimation, 56px music button, 240px+ bank QR with Vietnamese instructions, 48px touch targets.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-performance/09-CONTEXT.md
@.planning/phases/09-polish-and-performance/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/web/app/w/[slug]/EnvelopeAnimation.tsx:
```typescript
interface EnvelopeAnimationProps {
  templateId: TemplateId
  groomName: string
  brideName: string
  guestName?: string
  onOpen: () => void
}

const ENVELOPE_COLORS: Record<TemplateId, { body: string; flap: string; seal: string; text: string }>

// Existing frame measurement infrastructure:
const frameTimesRef = useRef<number[]>([])
const lastFrameRef = useRef<number>(0)
const measureFrameTime = useCallback(() => { ... }, [])
const getConfettiParticleCount = useCallback(() => { ... }, [])
```

From apps/web/app/w/[slug]/MusicPlayer.tsx:
```typescript
interface MusicPlayerProps {
  musicUrl: string
  autoStart?: boolean
}
// Current button: h-12 w-12 (48px) -- needs increase to h-14 w-14 (56px)
// Current equalizer bars: w-[3px] heights 12-18px -- needs increase
```

From apps/web/components/templates/TemplateTraditional.tsx (same pattern in Modern, Minimalist):
```typescript
// Bank QR currently: max-w-[140px] -- needs increase to max-w-[240px]
// Uses <BankQrLock> wrapper for free tier blur
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Performance gate and CSS-only fallback for envelope animation</name>
  <files>apps/web/app/w/[slug]/EnvelopeAnimationFallback.tsx, apps/web/app/w/[slug]/EnvelopeAnimation.tsx, apps/web/__tests__/components/EnvelopeAnimation.test.tsx</files>
  <behavior>
    - Test 1: EnvelopeAnimationFallback renders sealed envelope with tap handler and skip button
    - Test 2: EnvelopeAnimationFallback calls onOpen after fade-out transition when tapped
    - Test 3: EnvelopeAnimationFallback skip button has min 48px touch target and is always visible
    - Test 4: EnvelopeAnimationFallback does NOT import framer-motion (verify via module mock)
    - Test 5: EnvelopeAnimation renders fallback when useFallback is true (mock rAF to simulate slow frames)
    - Test 6: Skip button is centered at bottom with "Bo qua >>" text, min 48px dimensions
  </behavior>
  <action>
    1. Create `EnvelopeAnimationFallback.tsx`: A pure CSS transition component that accepts the same props as EnvelopeAnimation (templateId, groomName, brideName, guestName, onOpen). Renders a static sealed envelope image using the same ENVELOPE_COLORS. On tap/click, fades out via CSS `transition-opacity duration-500` and calls `onOpen` after 500ms via setTimeout. Includes always-visible "Bo qua >>" skip button centered at bottom (`left-1/2 -translate-x-1/2`) with `min-h-12 min-w-12` (48px) touch target, `text-sm font-medium`. NO framer-motion import. NO particles. NO 3D transforms. Uses the same visual structure as EnvelopeAnimation (envelope body, flap, wax seal, greeting text) but all static divs with no motion.div.

    2. Modify `EnvelopeAnimation.tsx`: Add a performance gate that runs BEFORE any animation. On mount, measure 8 requestAnimationFrame frame times. If average > 20ms, set `useFallback = true` and render `<EnvelopeAnimationFallback>` instead. While measuring (useFallback === null), render the sealed envelope statically (no animation yet -- the user sees the envelope, not a blank screen). Implementation:
       - Add `const [useFallback, setUseFallback] = useState<boolean | null>(null)`
       - useEffect that runs 8 rAF frames, computes average, sets useFallback
       - `if (useFallback === null)` render static sealed envelope (same JSX as sealed state but without motion controls)
       - `if (useFallback === true)` render `<EnvelopeAnimationFallback {...props} />`
       - Otherwise render the existing framer-motion animation as-is

    3. Update skip button in EnvelopeAnimation (the non-fallback path): Change from `absolute bottom-8 right-8` to `absolute bottom-8 left-1/2 -translate-x-1/2`, increase to `min-h-12 min-w-12 px-6 py-3 text-sm font-medium`. Always visible from start (no delay).

    4. Update/extend existing tests in EnvelopeAnimation.test.tsx to cover fallback rendering and performance gate.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run __tests__/components/EnvelopeAnimation.test.tsx -x</automated>
  </verify>
  <done>Performance gate measures frame times on mount; slow devices get CSS-only fallback; skip button is centered and 48px+; EnvelopeAnimationFallback exists as separate component without framer-motion dependency</done>
</task>

<task type="auto">
  <name>Task 2: Elderly-friendly music player and bank QR improvements</name>
  <files>apps/web/app/w/[slug]/MusicPlayer.tsx, apps/web/components/templates/TemplateTraditional.tsx, apps/web/components/templates/TemplateModern.tsx, apps/web/components/templates/TemplateMinimalist.tsx</files>
  <action>
    1. Update `MusicPlayer.tsx`:
       - Increase button size from `h-12 w-12` to `h-14 w-14` (56px per CONTEXT.md decision)
       - Increase equalizer bar widths from `w-[3px]` to `w-[4px]`
       - Increase equalizer bar heights proportionally (16px, 22px, 14px instead of 14px, 18px, 12px)
       - Increase Play icon from `h-5 w-5` to `h-6 w-6`
       - Ensure button has `min-h-14 min-w-14` for guaranteed 56px touch target

    2. Update all 3 template components (TemplateTraditional.tsx, TemplateModern.tsx, TemplateMinimalist.tsx):
       - Change bank QR image from `max-w-[140px]` to `w-full max-w-[240px]` (responsive, scales down on tiny screens, hits 240px on normal phones)
       - Add Vietnamese instruction text below each bank QR image: `<p className="mt-2 text-center text-sm text-gray-500">Mo ung dung ngan hang va quet ma QR</p>`
       - Instruction text goes inside the same container div as the QR image, after the img tag
       - Apply consistently to both groom-side (bankQrUrl) and bride-side (brideBankQrUrl) QR sections in each template

    3. Ensure all interactive elements in the public page meet the 48px minimum touch target:
       - QR code section in InvitationShell: the existing `h-40 w-40` (160px) is already sufficient
       - Skip button: already handled in Task 1
       - Music button: handled above with h-14 w-14 (56px)
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run __tests__/components/MusicPlayer.test.tsx __tests__/components/templates.test.tsx -x</automated>
  </verify>
  <done>Music button is 56px with larger equalizer bars; all 3 templates show bank QR at 240px+ with "Mo ung dung ngan hang va quet ma QR" instruction text; all touch targets meet 48px minimum</done>
</task>

</tasks>

<verification>
- EnvelopeAnimationFallback renders without importing framer-motion
- Performance gate correctly detects slow frames and switches to fallback
- Skip button is centered, always visible, 48px+ touch target
- Music button is 56px (h-14 w-14)
- Bank QR is 240px+ with Vietnamese instruction in all 3 templates
- All existing tests continue to pass: `cd apps/web && pnpm vitest run --reporter=verbose`
</verification>

<success_criteria>
1. EnvelopeAnimationFallback.tsx exists as a CSS-only fallback with no framer-motion dependency
2. EnvelopeAnimation.tsx gates on frame time measurement (8 frames, >20ms avg = fallback)
3. MusicPlayer button is 56px with larger equalizer bars
4. All 3 templates show bank QR at max-w-[240px] with elderly-friendly instruction text
5. All touch targets >= 48px on public page
6. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-performance/09-01-SUMMARY.md`
</output>
