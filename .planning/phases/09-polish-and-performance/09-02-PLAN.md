---
phase: 09-polish-and-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/invitations/expiry/expiry-cron.service.ts
  - apps/api/src/invitations/invitations.module.ts
  - apps/api/src/invitations/invitations.service.ts
  - apps/api/src/invitations/public-invitations.controller.ts
  - apps/api/src/invitations/__tests__/expiry-cron.spec.ts
autonomous: true
requirements:
  - PUBL-10

must_haves:
  truths:
    - "Published invitations auto-expire after the latest ceremony date + 7-day grace period without manual admin action"
    - "Expired invitations show the thank-you page (not 404) on the public URL"
    - "Cron runs daily at midnight Vietnam time (UTC+7) and triggers ISR revalidation for each expired slug"
    - "The runtime expiry check in findBySlug remains as a safety net alongside the cron"
  artifacts:
    - path: "apps/api/src/invitations/expiry/expiry-cron.service.ts"
      provides: "Daily cron job marking expired invitations"
      min_lines: 20
      contains: "@Cron"
    - path: "apps/api/src/invitations/invitations.service.ts"
      provides: "markExpired method for batch expiry processing"
      contains: "markExpired"
    - path: "apps/api/src/invitations/__tests__/expiry-cron.spec.ts"
      provides: "Unit tests for cron service"
      min_lines: 15
  key_links:
    - from: "apps/api/src/invitations/expiry/expiry-cron.service.ts"
      to: "apps/api/src/invitations/invitations.service.ts"
      via: "DI injection, calls markExpired()"
      pattern: "this\\.invitations\\.markExpired"
    - from: "apps/api/src/invitations/invitations.service.ts"
      to: "triggerRevalidation"
      via: "ISR revalidation after marking expired"
      pattern: "triggerRevalidation.*slug"
---

<objective>
Implement auto-expiry cron job that marks published invitations as expired after the latest ceremony date + 7-day grace period, and triggers ISR revalidation so the public URL shows the thank-you page.

Purpose: Invitations auto-expire on schedule without manual admin action. The public URL switches to the thank-you page within the configured grace period after the wedding date. This fulfills the "auto-expiry without manual action" success criterion from Phase 9.

Output: ExpiryCronService with daily midnight UTC+7 schedule, markExpired method on InvitationsService, findBySlug updated to handle expired status.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-performance/09-CONTEXT.md
@.planning/phases/09-polish-and-performance/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/api/src/invitations/invitations.service.ts:
```typescript
// findBySlug currently filters: .in('status', ['published', 'save_the_date'])
// Runtime expiry check computes expired from ceremony dates + 7-day grace
// triggerRevalidation(slug) is a private method that calls Next.js revalidation API
// supabaseAdmin.client is available for direct DB queries

// Relevant existing method signature:
async findBySlug(slug: string): Promise<Invitation & { expired: boolean; isSaveTheDate: boolean; musicUrl?: string }>
```

From apps/api/src/invitations/invitations.module.ts:
```typescript
@Module({
  imports: [SupabaseModule],
  controllers: [InvitationsController, PublicInvitationsController],
  providers: [InvitationsService],
})
export class InvitationsModule {}
```

NestJS Schedule Pattern (from @nestjs/schedule):
```typescript
import { ScheduleModule } from '@nestjs/schedule'
import { Cron } from '@nestjs/schedule'

// Module: imports: [ScheduleModule.forRoot()]
// Service: @Cron('0 0 * * *', { timeZone: 'Asia/Ho_Chi_Minh' })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @nestjs/schedule and create expiry cron service with markExpired</name>
  <files>apps/api/src/invitations/expiry/expiry-cron.service.ts, apps/api/src/invitations/invitations.module.ts, apps/api/src/invitations/invitations.service.ts</files>
  <action>
    1. Install dependency: `cd apps/api && pnpm add @nestjs/schedule`

    2. Add `markExpired()` method to `invitations.service.ts`:
       - Query all invitations with `status = 'published'` and `deleted_at IS NULL`
       - For each, compute expiry using the LATER of groom_ceremony_date and bride_ceremony_date + 7-day grace period (same logic as findBySlug runtime check)
       - Filter to those that are expired (latest ceremony date + 7 days < now)
       - Update each expired invitation's status to `'expired'`
       - Call `triggerRevalidation(slug)` for each (non-blocking, wrapped in try/catch with logger.warn)
       - Return the count of newly expired invitations
       - Make `triggerRevalidation` accessible: if currently private, keep it private and call from within the service. markExpired is a public method on InvitationsService.

    3. Update `findBySlug` in `invitations.service.ts`:
       - Add `'expired'` to the status filter: `.in('status', ['published', 'save_the_date', 'expired'])`
       - When status is `'expired'`, set `expired = true` (skip the runtime date calculation since cron already determined it)
       - The runtime date-based expiry check remains as a safety net for invitations the cron hasn't processed yet
       - Expired invitations return `expired: true` -- the frontend already handles this by showing ThankYouPage

    4. Create `apps/api/src/invitations/expiry/expiry-cron.service.ts`:
       - Injectable service with `@Cron('0 0 * * *', { name: 'auto-expire-invitations', timeZone: 'Asia/Ho_Chi_Minh' })` decorator
       - Injects `InvitationsService`
       - Uses `Logger` for logging start/completion/count
       - Calls `this.invitationsService.markExpired()` in the cron handler

    5. Update `invitations.module.ts`:
       - Add `ScheduleModule.forRoot()` to imports
       - Add `ExpiryCronService` to providers
       - Import both from their respective paths
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run src/invitations/__tests__/ -x</automated>
  </verify>
  <done>@nestjs/schedule installed; ExpiryCronService runs daily at midnight UTC+7; markExpired method processes expired invitations and triggers ISR revalidation; findBySlug handles 'expired' status correctly; existing tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for expiry cron service and markExpired</name>
  <files>apps/api/src/invitations/__tests__/expiry-cron.spec.ts</files>
  <behavior>
    - Test 1: markExpired marks a published invitation as expired when ceremony date + 7 days has passed
    - Test 2: markExpired does NOT expire a published invitation when ceremony date + 7 days has NOT passed
    - Test 3: markExpired uses the LATER of groom/bride ceremony dates for dual-family invitations
    - Test 4: markExpired triggers revalidation for each expired slug
    - Test 5: markExpired skips save_the_date invitations (teasers don't expire)
    - Test 6: ExpiryCronService.handleExpiry calls markExpired and logs the count
  </behavior>
  <action>
    Create `apps/api/src/invitations/__tests__/expiry-cron.spec.ts` with unit tests for the expiry cron. Mock `SupabaseAdminService` and its client to simulate DB responses:
    - Mock `supabaseAdmin.client.from('invitations').select().eq().is()` to return test invitation data
    - Mock `supabaseAdmin.client.from('invitations').update().eq()` to verify status change
    - Test various scenarios: single ceremony date expired, dual dates with later one not expired, no ceremony dates, disabled invitations
    - Mock `triggerRevalidation` (or verify via the revalidation fetch mock) to confirm it's called
    - Test the ExpiryCronService wrapper that delegates to InvitationsService.markExpired

    Follow existing test patterns from `apps/api/src/invitations/__tests__/` -- use `vi.fn()` for mocks, `describe/it` blocks.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run src/invitations/__tests__/expiry-cron.spec.ts -x</automated>
  </verify>
  <done>6+ tests covering markExpired logic (expired/not-expired/dual-dates/revalidation/teasers) and cron service wrapper; all pass</done>
</task>

</tasks>

<verification>
- `@nestjs/schedule` is in apps/api/package.json dependencies
- ExpiryCronService is registered in InvitationsModule
- ScheduleModule.forRoot() is imported in InvitationsModule
- markExpired correctly handles dual ceremony dates with 7-day grace period
- findBySlug includes 'expired' in status filter and returns expired: true for expired invitations
- Cron runs at midnight UTC+7 (0 0 * * * with Asia/Ho_Chi_Minh timezone)
- triggerRevalidation called non-blocking for each expired slug
- All existing invitations service tests still pass
- New expiry cron tests all pass
- Full API test suite: `cd apps/api && pnpm vitest run --reporter=verbose`
</verification>

<success_criteria>
1. @nestjs/schedule installed and ScheduleModule.forRoot() imported in InvitationsModule
2. ExpiryCronService exists with @Cron at midnight UTC+7
3. markExpired method processes published invitations past ceremony date + 7-day grace
4. findBySlug handles 'expired' status (returns expired: true, shows thank-you page)
5. Runtime expiry check in findBySlug remains as safety net
6. Unit tests for expiry logic pass
7. All existing API tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-performance/09-02-SUMMARY.md`
</output>
