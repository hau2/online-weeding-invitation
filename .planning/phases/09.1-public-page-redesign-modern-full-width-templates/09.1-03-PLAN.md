---
phase: 09.1-public-page-redesign-modern-full-width-templates
plan: 03
type: execute
wave: 3
depends_on: ["09.1-02"]
files_modified:
  - apps/web/app/w/[slug]/InvitationShell.tsx
  - apps/web/app/w/[slug]/FallingPetals.tsx
  - apps/web/app/w/[slug]/ThankYouPage.tsx
  - apps/web/app/w/[slug]/SaveTheDatePage.tsx
  - apps/web/app/w/[slug]/page.tsx
autonomous: true
requirements:
  - SHELL-01
must_haves:
  truths:
    - "InvitationShell no longer wraps in DesktopFrame — revealed invitation is full-width"
    - "Envelope animation still renders centered ~400px (unchanged per locked decision)"
    - "After envelope opens, SharedTemplate renders in full-width layout with StickyNav"
    - "FallingPetals accepts theme petal colors instead of templateId lookup"
    - "ThankYouPage and SaveTheDatePage use theme system instead of TEMPLATE_COLORS records"
    - "Falling petals continue during both envelope and revealed stages"
  artifacts:
    - path: "apps/web/app/w/[slug]/InvitationShell.tsx"
      provides: "Updated shell: removes DesktopFrame, renders SharedTemplate + StickyNav full-width"
      contains: "SharedTemplate"
    - path: "apps/web/app/w/[slug]/FallingPetals.tsx"
      provides: "Updated: accepts petalColors + petalEnabled instead of templateId"
    - path: "apps/web/app/w/[slug]/ThankYouPage.tsx"
      provides: "Updated: uses getTheme() instead of TEMPLATE_COLORS"
    - path: "apps/web/app/w/[slug]/SaveTheDatePage.tsx"
      provides: "Updated: uses getTheme() instead of TEMPLATE_COLORS"
  key_links:
    - from: "apps/web/app/w/[slug]/InvitationShell.tsx"
      to: "apps/web/components/templates/SharedTemplate.tsx"
      via: "imports and renders SharedTemplate with theme"
      pattern: "import.*SharedTemplate"
    - from: "apps/web/app/w/[slug]/InvitationShell.tsx"
      to: "apps/web/components/templates/themes/index.ts"
      via: "imports getTheme to resolve theme from templateId"
      pattern: "import.*getTheme.*themes"
    - from: "apps/web/app/w/[slug]/FallingPetals.tsx"
      to: "apps/web/components/templates/themes/index.ts"
      via: "No longer needed — receives colors directly"
---

<objective>
Rewire InvitationShell to render the new full-width SharedTemplate after envelope opens, remove DesktopFrame entirely, update FallingPetals to use theme petal colors, and update ThankYouPage/SaveTheDatePage to use the theme system.

Purpose: This connects the new template system to the actual public invitation page, completing the envelope-to-fullwidth transition that is the core UX change of this phase.
Output: Updated InvitationShell, FallingPetals, ThankYouPage, SaveTheDatePage
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-CONTEXT.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-RESEARCH.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-01-SUMMARY.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and Plan 02 outputs -->

From apps/web/components/templates/themes/index.ts:
```typescript
export interface ThemeConfig { /* all properties */ }
export function getTheme(templateId: string): ThemeConfig
```

From apps/web/components/templates/SharedTemplate.tsx:
```typescript
export function SharedTemplate({ invitation, theme, className, ceremonyDate, ceremonyTime }: SharedTemplateProps)
```

From apps/web/components/templates/sections/StickyNav.tsx:
```typescript
export function StickyNav({ invitation, theme }: SectionProps)
```

From apps/web/app/w/[slug]/InvitationShell.tsx (CURRENT — to be rewritten):
```typescript
// Uses: DesktopFrame, TemplateRenderer, CountdownTimer, FallingPetals, MusicPlayer, Watermark
// ENVELOPE_BG record keyed by TemplateId
// Dynamic imports for EnvelopeAnimation, FallingPetals, MusicPlayer, CountdownTimer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FallingPetals to accept theme colors directly</name>
  <files>apps/web/app/w/[slug]/FallingPetals.tsx</files>
  <action>
Update FallingPetals component to accept petal colors directly from the theme config instead of looking up colors via templateId.

**Change the props interface from:**
```typescript
interface FallingPetalsProps {
  templateId: TemplateId
  enabled?: boolean
}
```

**To:**
```typescript
interface FallingPetalsProps {
  petalColors?: string[]
  enabled?: boolean
}
```

- Remove the `PETAL_COLORS` record (no longer needed — colors come from ThemeConfig.petalColors)
- Change `const colors = PETAL_COLORS[templateId]` to `const colors = petalColors ?? ['#FFD1DC', '#FFC0CB', '#FFE4E1', '#FFFFFF']` (default to soft pink if not provided)
- Remove the `TemplateId` import
- Keep ALL existing animation logic unchanged (seeded PRNG, PETAL_COUNT, keyframes, buildPetals, etc.)
- If `enabled` is false OR `petalColors` is empty array, return null
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>FallingPetals accepts petalColors prop instead of templateId, removes PETAL_COLORS lookup, returns null when petalColors is empty</done>
</task>

<task type="auto">
  <name>Task 2: Rewire InvitationShell, ThankYouPage, and SaveTheDatePage</name>
  <files>
    apps/web/app/w/[slug]/InvitationShell.tsx,
    apps/web/app/w/[slug]/ThankYouPage.tsx,
    apps/web/app/w/[slug]/SaveTheDatePage.tsx,
    apps/web/app/w/[slug]/page.tsx
  </files>
  <action>
**1. Rewrite InvitationShell.tsx:**

Major changes:
- Remove `import { DesktopFrame } from './DesktopFrame'` — no longer used
- Remove `import { TemplateRenderer } from '@/components/templates/TemplateRenderer'` — replaced by SharedTemplate
- Remove `ENVELOPE_BG` record — replace with theme-derived background
- Remove the `CountdownTimer` dynamic import — countdown is now inside SharedTemplate via CountdownSection
- Add: `import { SharedTemplate } from '@/components/templates/SharedTemplate'`
- Add: `import { StickyNav } from '@/components/templates/sections/StickyNav'`
- Add: `import { getTheme } from '@/components/templates/themes'`
- Add: `import { plusJakartaSans } from '@/lib/fonts'`

At the top of the component function:
```typescript
const theme = getTheme(invitation.templateId)
```

**Envelope stage (envelopeOpened === false):**
- Remove DesktopFrame wrapper entirely
- Render a centered container: `<div className="min-h-screen flex items-center justify-center" style={{ backgroundColor: theme.backgroundColor }}>`
- FallingPetals: change from `templateId={invitation.templateId}` to `petalColors={theme.petalColors} enabled={theme.petalEnabled}`
- EnvelopeAnimation stays exactly as-is (centered ~400px with wax seal — locked decision)
- Watermark stays as-is for free tier

**Revealed stage (envelopeOpened === true):**
- Remove DesktopFrame wrapper entirely
- Wrap everything in a full-width div with Plus Jakarta Sans font class: `<div className={cn('relative min-h-screen', plusJakartaSans.variable, 'font-[family-name:var(--font-display)]')}>`
- FallingPetals: `petalColors={theme.petalColors} enabled={theme.petalEnabled}` (continues during revealed stage per locked decision)
- MusicPlayer stays as-is
- Use framer-motion fade-in-up transition (existing pattern: `initial={{ opacity: 0, y: 30 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.8, ease: 'easeOut' }}`)
- Inside the animated div, render:
  1. `<StickyNav invitation={filteredInvitation} theme={theme} />` — OUTSIDE the scrollable content
  2. `<SharedTemplate invitation={filteredInvitation} theme={theme} ceremonyDate={ceremonyDate} ceremonyTime={ceremonyTime} />` — replaces TemplateRenderer + CountdownTimer + QR code section + old footer
- Remove the old QR code rendering section (moved into template or removed — QR was a small section that can go)
- Remove the old footer (`<footer>` with "Thiep cuoi duoc tao boi") — now in FooterSection
- Watermark stays as-is for free tier (z-[60])

**Z-index stacking order:**
- FallingPetals: z-30 (lowered from z-40 to sit below nav)
- StickyNav: z-40
- MusicPlayer: z-50
- Watermark: z-[60]

Update the FallingPetals z-index: in the FallingPetals component (already modified in Task 1), change `style={{ zIndex: 40 }}` to `style={{ zIndex: 30 }}`.

**2. Update ThankYouPage.tsx:**
- Remove the old `TEMPLATE_COLORS` record
- Import `getTheme` from themes index
- Derive colors from theme: `const theme = getTheme(invitation.templateId)`
- Map theme colors to the existing rendering (bg gradient, heading color, text color, accent)
- Apply Plus Jakarta Sans font class
- Keep the existing layout structure (simple centered page)

**3. Update SaveTheDatePage.tsx:**
- Remove the old `TEMPLATE_COLORS` record
- Import `getTheme` from themes index
- Derive colors from theme: `const theme = getTheme(invitation.templateId)`
- Map theme colors to the existing rendering
- Apply Plus Jakarta Sans font class
- Update the CountdownTimer reference: the old CountdownTimer is still used in SaveTheDatePage (it's a different page, not the main invitation). Keep importing from `./CountdownTimer` for now — the old CountdownTimer still exists and works. SaveTheDatePage is a simple teaser, not the full template.
- Keep the existing layout structure

**4. Update page.tsx (if needed):**
- Check if the public page.tsx applies font classes or layout wrappers that need updating. The Plus Jakarta Sans font variable needs to be available. If the layout wraps content with a class, ensure the font-display variable is applied at the right level.
- If page.tsx wraps in a Suspense boundary (it should from Phase 5 P06), ensure that still works with the new components.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - InvitationShell renders SharedTemplate + StickyNav after envelope opens (no DesktopFrame)
    - Envelope stage renders in centered container with theme background color
    - Revealed stage is full-width with Plus Jakarta Sans font
    - FallingPetals uses theme.petalColors, renders at z-30
    - StickyNav at z-40, MusicPlayer at z-50, Watermark at z-60
    - ThankYouPage uses getTheme() instead of TEMPLATE_COLORS
    - SaveTheDatePage uses getTheme() instead of TEMPLATE_COLORS
    - Existing tests still pass (may need test mock updates for changed dynamic import order)
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd apps/web && npx tsc --noEmit`
- Tests pass: `cd apps/web && npx vitest run --reporter=verbose`
- No DesktopFrame import in InvitationShell: `grep -r "DesktopFrame" apps/web/app/w/[slug]/InvitationShell.tsx` returns nothing
</verification>

<success_criteria>
- InvitationShell uses SharedTemplate instead of TemplateRenderer
- DesktopFrame is no longer imported or used in InvitationShell
- Envelope animation is unchanged (centered ~400px)
- After envelope opens, full-width SharedTemplate renders with theme styling
- FallingPetals continues during both stages with theme-specific colors
- StickyNav renders on desktop after envelope opens
- ThankYouPage and SaveTheDatePage use the new theme system
- Z-index order: petals(30) < nav(40) < music(50) < watermark(60)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-03-SUMMARY.md`
</output>
