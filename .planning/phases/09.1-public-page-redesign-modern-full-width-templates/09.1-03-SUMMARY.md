---
phase: 09.1-public-page-redesign-modern-full-width-templates
plan: 03
subsystem: ui
tags: [react, tailwind, theme-config, framer-motion, next-font, shared-template, sticky-nav]

# Dependency graph
requires:
  - phase: 09.1-01
    provides: ThemeConfig interface, 6 theme configs, getTheme() resolver
  - phase: 09.1-02
    provides: SharedTemplate layout, StickyNav component, 7 section components
provides:
  - InvitationShell renders full-width SharedTemplate + StickyNav after envelope opens (no DesktopFrame)
  - FallingPetals accepts theme petalColors/petalEnabled instead of templateId lookup
  - ThankYouPage and SaveTheDatePage use getTheme() instead of TEMPLATE_COLORS records
  - Z-index stacking order established (petals 30, nav 40, music 50, watermark 60)
affects: [09.1-04, 09.1-05, editor-preview, public-invitation-page]

# Tech tracking
tech-stack:
  added: []
  patterns: [Theme-driven FallingPetals via petalColors prop, Full-width revealed stage with Plus Jakarta Sans font variable, StickyNav outside scroll container]

key-files:
  created: []
  modified:
    - apps/web/app/w/[slug]/FallingPetals.tsx
    - apps/web/app/w/[slug]/InvitationShell.tsx
    - apps/web/app/w/[slug]/ThankYouPage.tsx
    - apps/web/app/w/[slug]/SaveTheDatePage.tsx
    - apps/web/__tests__/components/InvitationContent.test.tsx
    - apps/web/__tests__/components/ThankYouPage.test.tsx

key-decisions:
  - "FallingPetals z-index lowered from 40 to 30 to sit below StickyNav (z-40)"
  - "InvitationShell envelope stage uses theme.backgroundColor instead of ENVELOPE_BG record"
  - "ThankYouPage uses inline styles with theme colors instead of Tailwind class-based TEMPLATE_COLORS record"
  - "SaveTheDatePage uses theme.primaryColor for photo border and accent text instead of per-template class records"
  - "CountdownTimer dynamic import removed from InvitationShell -- countdown is now inside SharedTemplate via CountdownSection"

patterns-established:
  - "Theme-driven FallingPetals: petalColors/petalEnabled props replace templateId lookup for direct theme integration"
  - "Full-width revealed stage: cn(plusJakartaSans.variable, font-family-name) pattern for font application"
  - "StickyNav rendered outside AnimatePresence motion.div for independent scroll behavior"

requirements-completed: [SHELL-01]

# Metrics
duration: 4min
completed: 2026-03-16
---

# Phase 9.1 Plan 03: Shell Integration Summary

**InvitationShell rewired to render full-width SharedTemplate + StickyNav after envelope opens, DesktopFrame removed, FallingPetals/ThankYouPage/SaveTheDatePage migrated to theme system**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-16T10:11:23Z
- **Completed:** 2026-03-16T10:15:39Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Removed DesktopFrame entirely from InvitationShell -- envelope stage is now a simple centered container, revealed stage is full-width
- Replaced TemplateRenderer + CountdownTimer + QR section + footer with single SharedTemplate component
- Added StickyNav for desktop navigation outside the content scroll container
- FallingPetals now accepts petalColors/petalEnabled directly from theme config, runs in both envelope and revealed stages
- ThankYouPage and SaveTheDatePage migrated from TEMPLATE_COLORS records to getTheme() theme system
- Updated test mocks for new component imports (SharedTemplate, StickyNav, themes, next/font/google)

## Task Commits

Each task was committed atomically:

1. **Task 1: Update FallingPetals to accept theme colors directly** - `e3a76ab` (feat)
2. **Task 2: Rewire InvitationShell, ThankYouPage, SaveTheDatePage** - `ad065d0` (feat)

## Files Created/Modified
- `apps/web/app/w/[slug]/FallingPetals.tsx` - Accepts petalColors/enabled instead of templateId, z-index 30
- `apps/web/app/w/[slug]/InvitationShell.tsx` - Full rewrite: SharedTemplate + StickyNav, no DesktopFrame, theme-driven
- `apps/web/app/w/[slug]/ThankYouPage.tsx` - Uses getTheme() with inline styles instead of TEMPLATE_COLORS
- `apps/web/app/w/[slug]/SaveTheDatePage.tsx` - Uses getTheme() with inline styles instead of TEMPLATE_COLORS
- `apps/web/__tests__/components/InvitationContent.test.tsx` - Updated mocks for SharedTemplate/StickyNav/themes/fonts
- `apps/web/__tests__/components/ThankYouPage.test.tsx` - Added next/font/google and themes mocks

## Decisions Made
- FallingPetals z-index lowered from 40 to 30 to ensure StickyNav (z-40) renders above petals
- Envelope stage uses `theme.backgroundColor` from getTheme() instead of a separate ENVELOPE_BG record
- ThankYouPage and SaveTheDatePage use inline styles (`style={{ color: theme.primaryColor }}`) instead of Tailwind class strings from a record, since theme properties are hex colors not Tailwind classes
- CountdownTimer dynamic import removed from InvitationShell since countdown is now rendered inside SharedTemplate via CountdownSection

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added next/font/google mock to test files**
- **Found during:** Task 2 (test verification)
- **Issue:** Tests importing InvitationShell/ThankYouPage transitively imported `@/lib/fonts` which calls `Be_Vietnam_Pro()` from `next/font/google`, causing `TypeError: Be_Vietnam_Pro is not a function` in Vitest
- **Fix:** Added `vi.mock('next/font/google', ...)` to both InvitationContent.test.tsx and ThankYouPage.test.tsx returning mock font objects with variable/className properties
- **Files modified:** `apps/web/__tests__/components/InvitationContent.test.tsx`, `apps/web/__tests__/components/ThankYouPage.test.tsx`
- **Verification:** All 93 tests pass (0 failures)
- **Committed in:** `ad065d0` (Task 2 commit)

**2. [Rule 3 - Blocking] Updated test mocks for new component imports**
- **Found during:** Task 2 (test verification)
- **Issue:** InvitationShell now imports SharedTemplate, StickyNav, and themes instead of TemplateRenderer -- old mocks targeted wrong modules
- **Fix:** Replaced `vi.mock('@/components/templates/TemplateRenderer')` with mocks for SharedTemplate, StickyNav, and themes. Updated dynamic mock order (3 components instead of 4 -- CountdownTimer removed). Updated test assertions for new component behavior (petals in both stages, SharedTemplate+StickyNav after opening)
- **Files modified:** `apps/web/__tests__/components/InvitationContent.test.tsx`
- **Verification:** All 6 InvitationShell tests pass
- **Committed in:** `ad065d0` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both auto-fixes necessary for tests to run with new imports. No scope creep.

## Issues Encountered
- Pre-existing TypeScript errors in CountdownTimer, DesktopFrame, EnvelopeAnimation, EnvelopeAnimationFallback from TemplateId extension in Plan 01 remain. These are documented in 09.1-01-SUMMARY.md and will be resolved in Plans 04-05 when those files are updated.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- InvitationShell is fully wired to the new template system -- SharedTemplate + StickyNav render after envelope opens
- DesktopFrame is still in the codebase but no longer imported by InvitationShell (can be removed in cleanup)
- Remaining TemplateId Record errors in CountdownTimer/EnvelopeAnimation/DesktopFrame to be fixed in Plans 04-05
- Editor preview components (Plan 04) can now test against the same theme system used in production

## Self-Check: PASSED

All 6 modified files verified present. Both task commits (e3a76ab, ad065d0) verified in git log.

---
*Phase: 09.1-public-page-redesign-modern-full-width-templates*
*Completed: 2026-03-16*
