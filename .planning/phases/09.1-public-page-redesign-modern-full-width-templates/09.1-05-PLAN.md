---
phase: 09.1-public-page-redesign-modern-full-width-templates
plan: 05
type: execute
wave: 4
depends_on: ["09.1-03", "09.1-04"]
files_modified:
  - apps/web/components/templates/TemplateTraditional.tsx
  - apps/web/components/templates/TemplateModern.tsx
  - apps/web/components/templates/TemplateMinimalist.tsx
  - apps/web/app/w/[slug]/DesktopFrame.tsx
  - apps/web/__tests__/components/InvitationContent.test.tsx
autonomous: false
requirements:
  - THEME-01
  - THEME-02
  - TMPL-01
  - SHELL-01
  - EDITOR-01
must_haves:
  truths:
    - "Old template components (TemplateTraditional, TemplateModern, TemplateMinimalist) are deleted"
    - "DesktopFrame.tsx is deleted"
    - "CountdownTimer.tsx is kept — SaveTheDatePage still imports it"
    - "No remaining imports of deleted files in the codebase"
    - "InvitationContent test updated for new component structure"
    - "Full web test suite passes after cleanup"
    - "User verifies the public invitation page visually matches Stitch reference"
  artifacts:
    - path: "apps/web/__tests__/components/InvitationContent.test.tsx"
      provides: "Updated test mocks for new InvitationShell structure (SharedTemplate instead of TemplateRenderer)"
  key_links:
    - from: "apps/web/components/templates/index.ts"
      to: "apps/web/components/templates/SharedTemplate.tsx"
      via: "No more references to deleted template components"
---

<objective>
Clean up deprecated files (old 3 template components, DesktopFrame), update InvitationContent tests for the new structure, and perform visual verification that the full public invitation page matches Stitch AI reference designs.

Purpose: Removes dead code, ensures test suite is green with the new architecture, and gets user sign-off on the visual result.
Output: Clean codebase with no dead imports, passing tests, user-verified visual result
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-CONTEXT.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-01-SUMMARY.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-02-SUMMARY.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-03-SUMMARY.md
@.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deprecated files, update tests, verify no broken imports</name>
  <files>
    apps/web/components/templates/TemplateTraditional.tsx,
    apps/web/components/templates/TemplateModern.tsx,
    apps/web/components/templates/TemplateMinimalist.tsx,
    apps/web/app/w/[slug]/DesktopFrame.tsx,
    apps/web/__tests__/components/InvitationContent.test.tsx
  </files>
  <action>
**1. Delete deprecated files:**
- `apps/web/components/templates/TemplateTraditional.tsx` — replaced by SharedTemplate + classic-red-gold theme
- `apps/web/components/templates/TemplateModern.tsx` — replaced by SharedTemplate + modern-red theme
- `apps/web/components/templates/TemplateMinimalist.tsx` — replaced by SharedTemplate + minimalist-bw theme
- `apps/web/app/w/[slug]/DesktopFrame.tsx` — removed entirely per locked decision (new full-width templates ARE the desktop experience)

**Keep CountdownTimer.tsx** — SaveTheDatePage still imports it for the standalone countdown on the teaser page (Plan 03 confirmed SaveTheDatePage keeps using CountdownTimer). Do NOT delete it.

**2. Verify no broken imports:**
Run `grep -r "TemplateTraditional\|TemplateModern\|TemplateMinimalist\|DesktopFrame" apps/web/ --include="*.tsx" --include="*.ts" -l` to find any remaining imports.

For each file found:
- If it's `index.ts` exports: ensure Plan 04 already removed these exports. If not, remove them now.
- If it's a test file: update the test to not reference deleted components.
- If it's any other file: the import should have been updated in Plan 03 or 04. Fix if missed.

**3. Update InvitationContent.test.tsx:**

The current test mocks `next/dynamic` with a fixed order assumption (0=EnvelopeAnimation, 1=FallingPetals, 2=MusicPlayer, 3=CountdownTimer). After Plan 03 changes:
- CountdownTimer is no longer dynamically imported (it's inside SharedTemplate)
- The dynamic import order may have changed

Update the test:
- Adjust the dynamicCallIndex mapping to match the new import order in InvitationShell (EnvelopeAnimation, FallingPetals, MusicPlayer — 3 dynamic imports instead of 4)
- Update the mock for `@/components/templates/TemplateRenderer`: since TemplateRenderer now renders SharedTemplate which has many sections, the mock should still return a simple div (testing the shell behavior, not the template content)
- Remove test assertions that reference DesktopFrame
- Update the "should show falling petals" test: FallingPetals now stays visible during BOTH envelope and revealed stages (not just envelope). Fix the assertion that checks `queryByTestId('falling-petals')` to expect it present after opening.
- Update mock invitation to use valid TemplateId: change from `templateId: 'traditional'` to `templateId: 'classic-red-gold'` (or keep 'traditional' since getTheme handles legacy mapping — either works)
- Remove unused fields from mock that don't exist on Invitation type (like `weddingDate`, `weddingTime`, `venueName`, `venueAddress` which were old field names replaced by groom*/bride* fields)
- Remove `groomFatherName`, `groomMotherName`, `brideFatherName`, `brideMotherName` if they're the wrong field names (the actual fields are `groomFather`, `groomMother`, `brideFather`, `brideMother`)

**4. Run full test suite:**
`cd apps/web && npx vitest run --reporter=verbose`

Fix any remaining failures caused by the deletions or import changes.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    - TemplateTraditional.tsx, TemplateModern.tsx, TemplateMinimalist.tsx deleted
    - DesktopFrame.tsx deleted
    - CountdownTimer.tsx kept (SaveTheDatePage still uses it)
    - No broken imports in codebase
    - InvitationContent.test.tsx updated for new InvitationShell structure
    - Full test suite passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of public page redesign</name>
  <files>N/A</files>
  <action>
Present the completed public page redesign for visual verification against Stitch AI reference designs.

What was built:
- 6 modern full-width templates replacing old 420px card-style templates
- Single SharedTemplate component with theme config system
- Desktop experience is the full-width template itself (no botanical frame)
- Envelope animation unchanged, transitions to full-width on open
- Plus Jakarta Sans font for public pages
- Editor shows 6 themes in visual grid with dual mobile+desktop preview mockups
  </action>
  <verify>User confirms visual match with Stitch AI reference designs</verify>
  <done>User types "approved" or describes issues to fix</done>
</task>

</tasks>

<verification>
- No broken imports: `grep -r "TemplateTraditional\|TemplateModern\|TemplateMinimalist\|DesktopFrame" apps/web/ --include="*.tsx" --include="*.ts" -l` returns nothing
- Full test suite: `cd apps/web && npx vitest run && cd ../api && npx vitest run`
- TypeScript compiles: `cd apps/web && npx tsc --noEmit`
</verification>

<success_criteria>
- All deprecated files deleted (3 old templates + DesktopFrame)
- CountdownTimer.tsx kept for SaveTheDatePage usage
- No broken imports anywhere in codebase
- InvitationContent tests updated and passing
- Full test suite green
- User visually confirms public page matches Stitch reference designs
- User confirms editor shows 6 themes and preview works
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-public-page-redesign-modern-full-width-templates/09.1-05-SUMMARY.md`
</output>
