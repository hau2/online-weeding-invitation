---
phase: 13-editor-ui-redesign-modern-stitch-ai-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/012_editor_new_fields.sql
  - packages/types/src/invitation.ts
  - apps/api/src/invitations/dto/update-invitation.dto.ts
  - apps/api/src/invitations/invitations.service.ts
  - apps/web/__tests__/components/EditorForm.test.tsx
  - apps/web/__tests__/components/CeremonyProgramEditor.test.tsx
  - apps/web/__tests__/components/AvatarUpload.test.tsx
  - apps/web/__tests__/components/PreviewShell.test.tsx
  - apps/web/__tests__/components/EditorShell.test.tsx
autonomous: true
requirements:
  - EDIT-UI-02
  - EDIT-UI-07
  - EDIT-UI-08

must_haves:
  truths:
    - "New DB columns exist for ceremony_program, groom_avatar_url, bride_avatar_url, groom_nickname, bride_nickname"
    - "Invitation TypeScript interface includes all 5 new fields"
    - "API accepts ceremony program and nickname fields via PATCH"
    - "Avatar URLs are NOT in FIELD_MAP (excluded like bankQrUrl)"
  artifacts:
    - path: "supabase/migrations/012_editor_new_fields.sql"
      provides: "DB schema for 5 new columns"
      contains: "ceremony_program"
    - path: "packages/types/src/invitation.ts"
      provides: "CeremonyProgramEvent interface + new Invitation fields"
      exports: ["CeremonyProgramEvent", "Invitation"]
    - path: "apps/api/src/invitations/dto/update-invitation.dto.ts"
      provides: "CeremonyProgramEventDto + nickname validation"
      contains: "CeremonyProgramEventDto"
    - path: "apps/api/src/invitations/invitations.service.ts"
      provides: "Updated FIELD_MAP, InvitationRow, mapRow, SELECT_ALL"
  key_links:
    - from: "packages/types/src/invitation.ts"
      to: "apps/api/src/invitations/invitations.service.ts"
      via: "CeremonyProgramEvent type import"
      pattern: "CeremonyProgramEvent"
    - from: "apps/api/src/invitations/dto/update-invitation.dto.ts"
      to: "apps/api/src/invitations/invitations.service.ts"
      via: "FIELD_MAP for ceremonyProgram, nicknames"
      pattern: "ceremonyProgram.*ceremony_program"
---

<objective>
Add 5 new database columns (ceremony_program, groom_avatar_url, bride_avatar_url, groom_nickname, bride_nickname), extend the Invitation TypeScript interface, update NestJS DTOs and service layer, and create Wave 0 test stubs for all Phase 13 components.

Purpose: Foundation layer that all subsequent editor redesign plans depend on. Without the types and API support, no frontend work can proceed.
Output: DB migration, updated types, updated API, test stubs for 5 components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-CONTEXT.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-RESEARCH.md

@packages/types/src/invitation.ts
@apps/api/src/invitations/dto/update-invitation.dto.ts
@apps/api/src/invitations/invitations.service.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/types/src/invitation.ts:
```typescript
export interface LoveStoryMilestone {
  date: string
  title: string
  description: string
}

export interface Invitation {
  // ... existing fields
  loveStory: LoveStoryMilestone[]
  // New fields will be added here
}
```

From apps/api/src/invitations/dto/update-invitation.dto.ts:
```typescript
class LoveStoryMilestoneDto {
  @IsString() date: string
  @IsString() @MaxLength(100) title: string
  @IsString() @MaxLength(300) description: string
}

class InvitationFieldsDto {
  // ... existing fields with @IsOptional + validators
  // New fields follow same pattern
}

export class UpdateInvitationDto extends PartialType(InvitationFieldsDto) {}
```

From apps/api/src/invitations/invitations.service.ts (4 places to sync):
```typescript
// 1. InvitationRow interface (snake_case DB shape)
// 2. FIELD_MAP (camelCase -> snake_case, excludes upload-managed URLs)
// 3. mapRow function (snake_case -> camelCase)
// 4. SELECT_ALL string (comma-separated column names)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DB migration + types + API DTO/service for 5 new fields</name>
  <files>
    supabase/migrations/012_editor_new_fields.sql,
    packages/types/src/invitation.ts,
    apps/api/src/invitations/dto/update-invitation.dto.ts,
    apps/api/src/invitations/invitations.service.ts
  </files>
  <action>
    1. Create `supabase/migrations/012_editor_new_fields.sql`:
       ```sql
       ALTER TABLE public.invitations
         ADD COLUMN IF NOT EXISTS ceremony_program JSONB NOT NULL DEFAULT '[]'::jsonb,
         ADD COLUMN IF NOT EXISTS groom_avatar_url TEXT DEFAULT NULL,
         ADD COLUMN IF NOT EXISTS bride_avatar_url TEXT DEFAULT NULL,
         ADD COLUMN IF NOT EXISTS groom_nickname TEXT NOT NULL DEFAULT '',
         ADD COLUMN IF NOT EXISTS bride_nickname TEXT NOT NULL DEFAULT '';
       ```

    2. Update `packages/types/src/invitation.ts`:
       - Add new interface `CeremonyProgramEvent` with fields: `time: string`, `title: string`, `description: string`
       - Export it alongside LoveStoryMilestone
       - Add to Invitation interface:
         - `ceremonyProgram: CeremonyProgramEvent[]`
         - `groomAvatarUrl: string | null`
         - `brideAvatarUrl: string | null`
         - `groomNickname: string`
         - `brideNickname: string`

    3. Update `apps/api/src/invitations/dto/update-invitation.dto.ts`:
       - Add `CeremonyProgramEventDto` nested class (same pattern as LoveStoryMilestoneDto):
         - `@IsString() time: string`
         - `@IsString() @MaxLength(100) title: string`
         - `@IsString() @MaxLength(300) description: string`
       - Add to InvitationFieldsDto:
         - `@IsOptional() @IsArray() @ArrayMaxSize(10) @ValidateNested({ each: true }) @Type(() => CeremonyProgramEventDto) ceremonyProgram: CeremonyProgramEventDto[]`
         - `@IsString() @MaxLength(50) groomNickname: string`
         - `@IsString() @MaxLength(50) brideNickname: string`
       - Do NOT add groomAvatarUrl or brideAvatarUrl to DTO (managed by upload endpoints per user decision)

    4. Update `apps/api/src/invitations/invitations.service.ts` — sync all 4 places:
       - **InvitationRow**: Add `ceremony_program: unknown[]`, `groom_avatar_url: string | null`, `bride_avatar_url: string | null`, `groom_nickname: string`, `bride_nickname: string`
       - **FIELD_MAP**: Add `ceremonyProgram: 'ceremony_program'`, `groomNickname: 'groom_nickname'`, `brideNickname: 'bride_nickname'`. Do NOT add avatar URLs (per pitfall #2 — follows bankQrUrl exclusion pattern).
       - **mapRow**: Add `ceremonyProgram: row.ceremony_program as CeremonyProgramEvent[]`, `groomAvatarUrl: row.groom_avatar_url`, `brideAvatarUrl: row.bride_avatar_url`, `groomNickname: row.groom_nickname`, `brideNickname: row.bride_nickname`. Import `CeremonyProgramEvent` from `@repo/types`.
       - **SELECT_ALL**: Add `ceremony_program, groom_avatar_url, bride_avatar_url, groom_nickname, bride_nickname` to the select string. Also add the missing `bank_account_number, bride_bank_account_number` (these exist in InvitationRow and FIELD_MAP but are currently missing from SELECT_ALL — fix while we're here).

    5. Update `useAutoSave.ts` exclusion destructure: Add `groomAvatarUrl` and `brideAvatarUrl` to the exclusion list alongside `photoUrls`, `bankQrUrl`, `brideBankQrUrl` (per pitfall #3).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -20 && cd apps/api && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Migration file exists with 5 ALTER TABLE ADD COLUMN statements
    - CeremonyProgramEvent interface exported from packages/types
    - Invitation interface has all 5 new fields
    - CeremonyProgramEventDto validates time/title/description with max 10 items
    - InvitationRow, FIELD_MAP, mapRow, SELECT_ALL all in sync for new columns
    - Avatar URLs excluded from FIELD_MAP and useAutoSave
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test stubs for Phase 13 components</name>
  <files>
    apps/web/__tests__/components/EditorForm.test.tsx,
    apps/web/__tests__/components/CeremonyProgramEditor.test.tsx,
    apps/web/__tests__/components/AvatarUpload.test.tsx,
    apps/web/__tests__/components/PreviewShell.test.tsx,
    apps/web/__tests__/components/EditorShell.test.tsx
  </files>
  <action>
    1. **Update** `apps/web/__tests__/components/EditorForm.test.tsx`:
       Replace the existing 5 todos (which reference old 3-section structure) with new todos matching the 9-section redesign:
       - `it.todo('renders 9 accordion sections in correct order')`
       - `it.todo('each section has icon header with correct icon and label')`
       - `it.todo('multiple sections can be open simultaneously')`
       - `it.todo('couple info section has groom/bride name, nickname inputs and avatar uploads')`
       - `it.todo('time and schedule section has ceremony dates and ceremony program editor')`
       - `it.todo('venue section has venue name, address, and Google Maps URL')`
       - `it.todo('calls onChange with changed field on input')`

    2. **Create** `apps/web/__tests__/components/CeremonyProgramEditor.test.tsx`:
       - `describe('CeremonyProgramEditor')` with:
       - `it.todo('renders empty state with add button when no events')`
       - `it.todo('renders existing events with time, title, description fields')`
       - `it.todo('adds a new blank event when add button clicked')`
       - `it.todo('removes event at given index when delete clicked')`
       - `it.todo('calls onChange with updated event on field change')`
       - `it.todo('disables add button when 10 events reached')`

    3. **Create** `apps/web/__tests__/components/AvatarUpload.test.tsx`:
       - `describe('AvatarUpload')` with:
       - `it.todo('renders circular placeholder when no avatar URL')`
       - `it.todo('renders avatar image when URL provided')`
       - `it.todo('opens file picker on click')`
       - `it.todo('calls onUpload with selected file')`

    4. **Create** `apps/web/__tests__/components/PreviewShell.test.tsx`:
       - `describe('PreviewShell')` with:
       - `it.todo('renders three tabs: Dien thoai, May tinh, Chia se lien ket')`
       - `it.todo('shows phone mockup by default')`
       - `it.todo('switches to desktop mockup when May tinh tab clicked')`
       - `it.todo('shows QR and share link in Chia se lien ket tab')`
       - `it.todo('has Quay lai chinh sua and Xuat ban ngay buttons')`

    5. **Create** `apps/web/__tests__/components/EditorShell.test.tsx`:
       - `describe('EditorShell')` with:
       - `it.todo('renders back arrow link to /dashboard')`
       - `it.todo('renders "Trinh chinh sua thiep" title')`
       - `it.todo('renders Mobile/Desktop preview toggle in center')`
       - `it.todo('renders save indicator showing auto-save status')`
       - `it.todo('renders Xem truoc and Xuat ban buttons in right section')`
       - `it.todo('toggles preview between phone and desktop mockup')`

    All test files follow existing pattern: `import { describe, it } from 'vitest'` only.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    - EditorForm test updated with 7 todos for 9-section structure
    - CeremonyProgramEditor test has 6 todos
    - AvatarUpload test has 4 todos
    - PreviewShell test has 5 todos
    - EditorShell test has 6 todos
    - All test files pass (todos are skipped, no failures)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for both web and api
- `cd apps/web && npx vitest run` passes (all new tests are todos)
- `cd apps/api && npx vitest run` passes
- Migration file is syntactically valid SQL
</verification>

<success_criteria>
- 5 new DB columns defined in migration 012
- CeremonyProgramEvent type exported from shared types
- Invitation interface has ceremonyProgram, groomAvatarUrl, brideAvatarUrl, groomNickname, brideNickname
- API DTO validates ceremonyProgram (max 10 items) and nicknames (max 50 chars)
- InvitationRow/FIELD_MAP/mapRow/SELECT_ALL are in sync
- Avatar URLs excluded from FIELD_MAP and useAutoSave
- 5 Wave 0 test stub files exist with appropriate todos
</success_criteria>

<output>
After completion, create `.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-01-SUMMARY.md`
</output>
