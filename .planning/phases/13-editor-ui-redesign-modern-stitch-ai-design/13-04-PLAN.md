---
phase: 13-editor-ui-redesign-modern-stitch-ai-design
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - apps/web/app/(app)/thep-cuoi/[id]/preview/page.tsx
  - apps/web/app/(app)/thep-cuoi/[id]/preview/PreviewShell.tsx
  - apps/web/components/templates/sections/TimelineSection.tsx
  - apps/web/app/(app)/thep-cuoi/[id]/FullPreviewDialog.tsx
autonomous: true
requirements:
  - EDIT-UI-05

must_haves:
  truths:
    - "Dedicated preview page exists at /thep-cuoi/[id]/preview"
    - "Preview page has three tabs: Dien thoai, May tinh, Chia se lien ket"
    - "Phone tab shows centered phone mockup, Desktop tab shows desktop mockup"
    - "Share tab shows invitation QR code and copyable link"
    - "Preview page has Quay lai chinh sua and Xuat ban ngay buttons"
    - "Ceremony program events render on public page in TimelineSection"
  artifacts:
    - path: "apps/web/app/(app)/thep-cuoi/[id]/preview/page.tsx"
      provides: "RSC preview page that fetches invitation"
      min_lines: 20
    - path: "apps/web/app/(app)/thep-cuoi/[id]/preview/PreviewShell.tsx"
      provides: "Client component with tabs, mockups, and action buttons"
      min_lines: 100
    - path: "apps/web/components/templates/sections/TimelineSection.tsx"
      provides: "Extended with ceremony program rendering"
  key_links:
    - from: "apps/web/app/(app)/thep-cuoi/[id]/preview/page.tsx"
      to: "apps/web/app/(app)/thep-cuoi/[id]/preview/PreviewShell.tsx"
      via: "RSC passes invitation to client component"
      pattern: "PreviewShell.*invitation"
    - from: "apps/web/app/(app)/thep-cuoi/[id]/preview/PreviewShell.tsx"
      to: "apps/web/components/templates/TemplateRenderer.tsx"
      via: "renders TemplateRenderer inside device mockup"
      pattern: "TemplateRenderer"
    - from: "apps/web/components/templates/sections/TimelineSection.tsx"
      to: "packages/types/src/invitation.ts"
      via: "reads ceremonyProgram from Invitation"
      pattern: "ceremonyProgram"
---

<objective>
Create the dedicated full-page preview route (replacing FullPreviewDialog) with Phone/Desktop/Share Link tabs, and extend the public page TimelineSection to render ceremony program events.

Purpose: Preview page gives users a proper full-screen preview before publishing. Ceremony program rendering ensures the new data field is visible on the public invitation.
Output: New preview route with PreviewShell client component, extended TimelineSection.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-CONTEXT.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-RESEARCH.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-01-SUMMARY.md

@apps/web/app/(app)/thep-cuoi/[id]/page.tsx
@apps/web/app/(app)/thep-cuoi/[id]/FullPreviewDialog.tsx
@apps/web/app/(app)/thep-cuoi/[id]/EditorPreview.tsx
@apps/web/components/templates/sections/TimelineSection.tsx
@apps/web/components/templates/TemplateRenderer.tsx
</context>

<interfaces>
<!-- Key types the executor needs -->

From apps/web/app/(app)/thep-cuoi/[id]/page.tsx (fetch pattern to reuse):
```typescript
async function getInvitation(id: string): Promise<Invitation | null> {
  const cookieStore = await cookies()
  const token = cookieStore.get('auth-token')?.value
  if (!token) return null
  const res = await fetch(`${apiUrl}/invitations/${id}`, {
    headers: { Cookie: `auth-token=${token}` },
    cache: 'no-store',
  })
  return res.json()
}
```

From apps/web/components/templates/TemplateRenderer.tsx:
```typescript
interface TemplateRendererProps {
  invitation: Invitation
}
export function TemplateRenderer({ invitation }: TemplateRendererProps): JSX.Element
```

From apps/web/components/templates/sections/TimelineSection.tsx:
```typescript
interface SectionProps {
  invitation: Invitation
  theme: ThemeConfig
}
export function TimelineSection({ invitation, theme }: SectionProps): JSX.Element | null
```

From packages/types/src/invitation.ts (after Plan 01):
```typescript
export interface CeremonyProgramEvent {
  time: string
  title: string
  description: string
}
export interface Invitation {
  ceremonyProgram: CeremonyProgramEvent[]
  // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Preview page route with PreviewShell tabs and mockups</name>
  <files>
    apps/web/app/(app)/thep-cuoi/[id]/preview/page.tsx,
    apps/web/app/(app)/thep-cuoi/[id]/preview/PreviewShell.tsx,
    apps/web/app/(app)/thep-cuoi/[id]/FullPreviewDialog.tsx
  </files>
  <action>
    **Create `preview/page.tsx` (RSC):**

    Follow the same fetch pattern as the parent `page.tsx`:
    - Import cookies, notFound
    - `getInvitation(id)` function — copy from parent page.tsx
    - Export default async function with `params: Promise<{ id: string }>`
    - Fetch invitation, notFound if null
    - Render `<PreviewShell invitation={invitation} />`

    **Create `preview/PreviewShell.tsx` (Client):**

    `'use client'` component with:

    **Props:** `{ invitation: Invitation }`

    **State:** `activeTab: 'phone' | 'desktop' | 'share'` (default: `'phone'`)
    **State:** `previewSide: 'groom' | 'bride'` (default: `'groom'`)

    **Layout (per Stitch preview.png screenshot):**

    ```
    Full-height flex column:
    ┌─────────────────────────────────────────────────────┐
    │ Top bar: heart icon + "Xem truoc thiep"  | buttons  │
    ├─────────────────────────────────────────────────────┤
    │ Tab bar: [Dien thoai] [May tinh] [Chia se lien ket]│
    │          [Nha trai] [Nha gai] (side toggle)         │
    ├─────────────────────────────────────────────────────┤
    │                                                     │
    │        Centered device mockup / share content       │
    │                                                     │
    └─────────────────────────────────────────────────────┘
    ```

    **Top bar:**
    - Left: Heart icon (red) + "Xem truoc thiep" title text
    - Right: `<Link href={`/thep-cuoi/${invitation.id}`}>` "Quay lai chinh sua" outlined button + Publish action button ("Xuat ban ngay" red filled button)
    - The publish button should use the same publish API call as PublishButton component. For simplicity, import and render the existing `<PublishButton>` component for the publish action.

    **Tab bar:**
    - Three tabs: "Dien thoai", "May tinh", "Chia se lien ket"
    - Active tab has underline border-b-2 border-red-500 + red text
    - Inactive tabs have gray text
    - Below tabs: small pill toggle for `[Nha trai | Nha gai]` side filtering

    **Side filtering:**
    Apply the same `useMemo` filtering logic as FullPreviewDialog:
    - Groom side: nullify bride ceremony/venue/bank fields
    - Bride side: nullify groom ceremony/venue/bank fields

    **Tab content:**

    1. **Dien thoai tab:** Large centered phone mockup (reuse EditorPreview phone mockup styling: 280px width, rounded corners, notch, device chrome). Inside: `<TemplateRenderer invitation={filtered} />` wrapped in scrollable container with Plus Jakarta Sans font. Gray background.

    2. **May tinh tab:** Large centered desktop mockup (reuse EditorPreview desktop styling: 580px+ width, browser chrome dots). Inside: `<TemplateRenderer invitation={filtered} />`. Gray background.

    3. **Chia se lien ket tab:**
       - If invitation has slug: show QR code image (from `invitation.qrCodeUrl`), copyable URL `${NEXT_PUBLIC_URL}/w/${slug}`, and two copy-link buttons (Nha trai / Nha gai with side param)
       - If no slug: show message "Xuat ban thiep de tao lien ket chia se"
       - Copy button with clipboard API + toast "Da sao chep lien ket"

    Import `TemplateRenderer` from `@/components/templates`, `plusJakartaSans` from `@/lib/fonts`, `cn` from `@/lib/utils`.

    **Delete FullPreviewDialog.tsx:**
    After creating the preview page, delete `FullPreviewDialog.tsx` since it's no longer imported anywhere (EditorShell will be updated in Plan 03 to use Link to preview page instead).

    Note: Plan 03 removes the FullPreviewDialog import from EditorShell. If Plan 03 runs first, the file is already unused. If Plan 04 runs first, the import removal will happen in Plan 03. Either way, deleting the file here is safe because we're replacing its functionality.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && npx vitest run __tests__/components/PreviewShell.test.tsx __tests__/components/FullPreviewDialog.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - preview/page.tsx fetches invitation via cookies and renders PreviewShell
    - PreviewShell has 3 tabs with correct labels
    - Phone tab shows phone mockup with TemplateRenderer
    - Desktop tab shows desktop mockup with TemplateRenderer
    - Share tab shows QR + copyable links (or message if unpublished)
    - Side toggle filters invitation data for groom/bride preview
    - "Quay lai chinh sua" links back to editor
    - FullPreviewDialog.tsx deleted
    - Tests pass (PreviewShell todos skip, FullPreviewDialog test may need update)
  </done>
</task>

<task type="auto">
  <name>Task 2: TimelineSection ceremony program rendering + public page avatar display</name>
  <files>
    apps/web/components/templates/sections/TimelineSection.tsx
  </files>
  <action>
    Extend `TimelineSection.tsx` to render ceremony program events below (or alongside) love story milestones.

    **Changes to TimelineSection:**

    After the existing love story rendering block, add a ceremony program section:

    1. Check `invitation.ceremonyProgram && invitation.ceremonyProgram.length > 0` — if empty, skip
    2. Add a "Lich trinh le cuoi" heading (same style as "Cau Chuyen Tinh Yeu" heading)
    3. Render ceremony program events in a timeline format similar to love story but with different icon:
       - Use `Clock` icon (from lucide-react) instead of `Heart` for ceremony events
       - Each event shows: time (bold, primary color), title (medium), description (muted)
       - Same vertical timeline line connecting events

    4. Update the section's null check: render if EITHER love story OR ceremony program has items (currently only checks love story)

    **Layout structure:**
    ```
    [Love Story section — if loveStory has items]
      Heading: "Cau Chuyen Tinh Yeu"
      Timeline with Heart icons

    [Ceremony Program section — if ceremonyProgram has items]
      Heading: "Lich Trinh Le Cuoi"
      Timeline with Clock icons
    ```

    Keep both sub-sections in the same `<section id="timeline">` wrapper. The two timelines are visually distinct via different icons and a spacing gap between them.

    Import `Clock` from `lucide-react` alongside existing `Heart`.

    Also render avatars in the HeroSection if present. Check if HeroSection exists and determine where avatars should display. If HeroSection exists, add avatar rendering there. If not, note this for a future plan — the priority here is ceremony program rendering.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && npx vitest run __tests__/components/sections.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TimelineSection renders both love story and ceremony program
    - Ceremony program shows Clock icons, love story shows Heart icons
    - Section renders if either love story or ceremony program has items
    - "Lich Trinh Le Cuoi" heading appears above ceremony events
    - Tests pass
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vitest run --reporter=verbose` passes
- Preview page accessible at /thep-cuoi/[id]/preview
- TimelineSection renders ceremony program events
- FullPreviewDialog.tsx is deleted
</verification>

<success_criteria>
- Preview page has 3 working tabs (phone mockup, desktop mockup, share link)
- Side toggle filters preview for groom/bride
- "Quay lai chinh sua" navigates back to editor
- Ceremony program events visible on public page timeline
- FullPreviewDialog replaced by dedicated route
</success_criteria>

<output>
After completion, create `.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-04-SUMMARY.md`
</output>
