---
phase: 13-editor-ui-redesign-modern-stitch-ai-design
plan: 05
type: execute
wave: 4
depends_on: ["13-03", "13-04"]
files_modified:
  - apps/web/__tests__/components/EditorForm.test.tsx
  - apps/web/__tests__/components/EditorShell.test.tsx
  - apps/web/__tests__/components/FullPreviewDialog.test.tsx
autonomous: false
requirements:
  - EDIT-UI-01
  - EDIT-UI-02
  - EDIT-UI-03
  - EDIT-UI-04
  - EDIT-UI-05
  - EDIT-UI-06
  - EDIT-UI-07
  - EDIT-UI-08

must_haves:
  truths:
    - "Full test suite passes after all phase changes"
    - "Editor form has 9 sections visually matching Stitch design"
    - "Top bar layout matches Stitch screenshots"
    - "Preview page works with all 3 tabs"
    - "New fields (avatars, nicknames, ceremony program) round-trip through editor"
  artifacts:
    - path: "apps/web/__tests__/components/EditorForm.test.tsx"
      provides: "Updated tests for 9-section structure"
    - path: "apps/web/__tests__/components/EditorShell.test.tsx"
      provides: "Tests for new top bar layout"
  key_links:
    - from: "apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx"
      to: "apps/web/app/(app)/thep-cuoi/[id]/EditorForm.tsx"
      via: "form rendered in left panel"
      pattern: "EditorForm"
    - from: "apps/web/app/(app)/thep-cuoi/[id]/EditorShell.tsx"
      to: "apps/web/app/(app)/thep-cuoi/[id]/preview/page.tsx"
      via: "Xem truoc link navigates to preview"
      pattern: "preview"
---

<objective>
Clean up test files, fix any test breakages from the EditorForm/EditorShell rewrites, remove the obsolete FullPreviewDialog test, and do a visual verification checkpoint to confirm the editor redesign matches Stitch design.

Purpose: Ensure all tests pass and the visual result matches expectations before marking the phase complete.
Output: Clean test suite, verified visual result.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-CONTEXT.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-RESEARCH.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-01-SUMMARY.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-02-SUMMARY.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-03-SUMMARY.md
@.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test suite — update/remove stale tests</name>
  <files>
    apps/web/__tests__/components/EditorForm.test.tsx,
    apps/web/__tests__/components/EditorShell.test.tsx,
    apps/web/__tests__/components/FullPreviewDialog.test.tsx
  </files>
  <action>
    1. **Delete** `apps/web/__tests__/components/FullPreviewDialog.test.tsx` — the component no longer exists (deleted in Plan 04). The test file imports from a deleted module and will fail.

    2. **Verify** that `EditorForm.test.tsx` and `EditorShell.test.tsx` contain only `it.todo()` stubs (created in Plan 01 Wave 0). If any test has been implemented (not just todo), ensure the imports point to the correct new file paths and module names.

    3. Run the full test suite: `cd apps/web && npx vitest run --reporter=verbose` and `cd apps/api && npx vitest run --reporter=verbose`. If any test fails due to:
       - Import of deleted FullPreviewDialog: remove or update the import
       - Changed EditorForm/EditorShell props: update test mocks
       - Missing CeremonyProgramEvent type: ensure @repo/types is up to date
       Fix each failure.

    4. Verify that `templates.test.tsx` and `sections.test.tsx` still pass after TimelineSection changes.

    5. Run final comprehensive check: `cd apps/web && npx vitest run && cd ../../apps/api && npx vitest run` — both must be green.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding/apps/web && npx vitest run --reporter=verbose 2>&1 | tail -50 && cd ../../apps/api && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - FullPreviewDialog.test.tsx deleted
    - All web tests pass (no import errors, no stale references)
    - All API tests pass
    - Zero test failures across the entire project
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — editor redesign matches Stitch design</name>
  <files>N/A</files>
  <action>
    User visually verifies the complete editor UI redesign against the Stitch AI reference design. This is the final gate for Phase 13.

    What was built:
    - Top bar with back arrow, "Trinh chinh sua thiep" title, center Mobile/Desktop toggle, right-side preview/publish buttons
    - 9 accordion sections with colored icon headers: Couple Info (with avatars + nicknames), Time and Schedule (with ceremony program), Venue (with Maps link), Photos, Music, Gift Money, Theme, Message + Love Story, Save the Date
    - Preview pane switches between phone and desktop mockup
    - Dedicated preview page at /thep-cuoi/[id]/preview with Phone/Desktop/Share tabs
    - New data fields: ceremony program events, groom/bride avatars, groom/bride nicknames
    - Neutral gray styling replacing rose theme (Stitch design)
    - Ceremony program renders on public page in timeline section
  </action>
  <verify>
    How to verify:
    1. Start dev server: `cd /Users/avepoint_admin/Downloads/online-weeding && pnpm dev`
    2. Log in and open an existing invitation editor
    3. Top bar check: Verify back arrow, title "Trinh chinh sua thiep", center Mobile/Desktop toggle, save indicator, preview + publish buttons on right
    4. Accordion check: Verify 9 sections with colored circle icons. Expand each to confirm content.
    5. Preview toggle: Click Mobile/Desktop toggle — preview pane should switch mockup type
    6. Preview page: Click "Xem truoc" — should navigate to full-page preview with Phone/Desktop/Share tabs
    7. New fields: Add ceremony program event, upload avatar, enter nickname — verify auto-save works
    8. Public page: Visit published URL, check ceremony program events in timeline section

    Resume signal: Type "approved" if the editor matches Stitch design, or describe issues.
  </verify>
  <done>
    - User confirms editor UI matches Stitch AI reference design
    - All 9 accordion sections visible and functional
    - Top bar layout correct
    - Preview page works with all 3 tabs
    - New fields save and display correctly
  </done>
</task>

</tasks>

<verification>
- Full test suite green (web + api)
- Visual verification by user confirms Stitch design match
- All 9 sections visible and functional
- New fields persist through auto-save
</verification>

<success_criteria>
- Zero test failures across entire project
- User approves visual match with Stitch AI reference design
- Editor form has 9 icon-based sections in correct order
- Top bar has correct Stitch layout
- Preview page works with all 3 tabs
- New data fields (ceremony program, avatars, nicknames) are functional end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/13-editor-ui-redesign-modern-stitch-ai-design/13-05-SUMMARY.md`
</output>
