---
phase: 15-admin-panel-redesign-modern-stitch-ai-design
plan: 04
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
  - 15-03
files_modified: []
autonomous: false
requirements:
  - ADMIN-LAYOUT
  - ADMIN-SIDEBAR
  - ADMIN-DASHBOARD
  - ADMIN-USERS
  - ADMIN-INVITATIONS
  - ADMIN-MUSIC
  - ADMIN-THEMES
  - ADMIN-PLANS
  - ADMIN-PAYMENTS
  - ADMIN-SETTINGS

must_haves:
  truths:
    - "All admin pages build without errors"
    - "No gray-* Tailwind classes remain in any admin file"
    - "Admin panel visually matches Stitch AI design system"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete admin panel redesign builds successfully and passes visual inspection.

Purpose: Final quality gate ensuring all 12 admin files are consistently restyled and the build is clean.
Output: Verified build + visual confirmation from user.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-panel-redesign-modern-stitch-ai-design/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and gray-* audit</name>
  <files></files>
  <action>
1. Run full build: `cd apps/web && npx next build` to confirm no TypeScript or build errors across all admin pages.
2. Run test suite: `cd apps/web && npx vitest run` to confirm no regressions.
3. Run gray-* audit across ALL admin files:
   ```
   grep -rn "gray-" apps/web/app/\(admin\)/ apps/web/components/admin/
   ```
   This MUST return empty. If any gray-* classes found, fix them in-place.
4. Verify no `focus:ring-gray` or `focus:ring-2 focus:ring-gray` patterns remain (must be replaced with `focus:border-[#ec1349]`).
5. Verify no `bg-gray-900` buttons remain (must be replaced with `bg-[#ec1349]`).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/online-weeding && grep -rn "gray-" apps/web/app/\(admin\)/ apps/web/components/admin/ 2>/dev/null; echo "EXIT:$?"</automated>
  </verify>
  <done>Build succeeds, tests pass, zero gray-* classes found in any admin file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete admin panel Stitch redesign</name>
  <files></files>
  <action>
Present the completed admin panel to the user for visual verification. The admin panel should show:
- Dark #181113 sidebar with "Thiep Cuoi Online" + "Admin" branding and #ec1349 active indicators
- Warm #f8f6f6 content area background
- Dashboard with colored stat card icons and #ec1349 chart line
- All list pages with Stitch card wrappers, #e6dbde borders, and Stitch status badges
- All inputs with #ec1349 focus states
- All primary buttons using #ec1349
- Settings page with per-card save buttons

Steps for the user:
1. Start dev server: `cd apps/web && npm run dev`
2. Visit http://localhost:3000/admin
3. Check sidebar: dark background, rose active indicator, branding
4. Check dashboard: colored stat icons, rose chart
5. Navigate to each page: /admin/nguoi-dung, /admin/thiep-cuoi, /admin/nhac, /admin/giao-dien, /admin/goi-dich-vu, /admin/thanh-toan, /admin/cai-dat
6. Verify all pages feel warm with rose accents, no gray remnants
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User approves the visual design of all admin pages as matching the Stitch AI design system.</done>
</task>

</tasks>

<verification>
- Build completes without errors
- All tests pass
- Zero gray-* classes in admin files
- User visually approves all admin pages
</verification>

<success_criteria>
Admin panel passes build verification, automated gray-* audit, and human visual inspection. All 8 admin page routes plus layout/sidebar consistently use the Stitch AI design system.
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-panel-redesign-modern-stitch-ai-design/15-04-SUMMARY.md`
</output>
