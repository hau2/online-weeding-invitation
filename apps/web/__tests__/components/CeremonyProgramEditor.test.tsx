import { describe, it } from 'vitest'

describe('CeremonyProgramEditor', () => {
  it.todo('renders empty state with add button when no events')
  it.todo('renders existing events with time, title, description fields')
  it.todo('adds a new blank event when add button clicked')
  it.todo('removes event at given index when delete clicked')
  it.todo('calls onChange with updated event on field change')
  it.todo('disables add button when 10 events reached')
})
